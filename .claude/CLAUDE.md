# Claude Code — Global Instructions

## Active Projects

### Finance Intelligence Dashboard (Wayfinder map)

<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!-- ▛▀▀ CURRENT STATE — READ THIS FIRST, IT IS THE AUTHORITATIVE TRUTH ▀▀▜ -->
<!-- Everything BELOW this block is historical layering kept for context.    -->
<!-- If this block and anything below disagree, THIS BLOCK WINS.             -->
<!-- Last updated: 2026-07-20 (CLEAN GitHub history; auth drizzle-hoisting fix; device-verified via EAS dev build; connect wizard VERIFIED end-to-end on-device — Plaid bank link works; PUSH-STATE: only the local.db purge is on GitHub, 12 later commits are local-only/unpushed). -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->

## ⇒ START HERE — Finance Dashboard current state (2026-07-20)

**Phase: BACKEND + FRONTEND BUILT and COMMITTED locally on top of a clean, purged base. PUSH-STATE (verified via `git ls-remote`): `origin/main` = `de8a414` — ONLY the local.db purge is on GitHub; the 12 feature/fix/docs commits since are LOCAL-ONLY and unpushed (prior "all pushed" notes were wrong). Push when the user OKs. Device-verified on Android via an EAS dev-client build. All three former "manual" tasks are DONE — the Plaid bank-connect flow was verified end-to-end on-device (accounts populated). The one remaining known issue is the WebView candlestick chart rendering blank on device (task #7).**

<!-- ▛▀▀ 2026-07-20 SESSION — SUPERSEDES stale "not pushed / paused / blocked" lines further below ▀▀▜ -->
**All three manual tasks tackled; everything is now on `origin/main` in a clean history.**

1. **local.db GitHub-history purge — DONE.** `git-filter-repo --invert-paths --path backend/local.db --force` (rewrote 22 commits) + `git push origin main --force` (`197898b`→`de8a414`); `origin/main` tree + full history verified clean of any `.db`. The 7 local-only frontend commits shipped in the same clean timeline. On-disk file untouched (gitignored). Repo is PRIVATE. **Windows clone reconcile deferred by user** — re-clone, or `git fetch && git checkout main && git reset --hard origin/main && git gc --prune=now`; do NOT `git pull`.

2. **Device verification — DONE (Android, on-device).** Real blocker chain fixed: ufw firewall (opened `8082` Metro + `3011` backend for `192.168.68.0/22`; 8081 is the home-automation orchestrator, left alone) → Expo Go too new for SDK 53 (current Go is SDK 57; sideloaded the SDK-53 Go APK from `expo.dev/go?sdkVersion=53`) → "remote update request not successful" = missing `frontend/assets/` (generated placeholder PNGs via sharp) + placeholder `eas.projectId` (removed). Screens render well ("impressed, polish later"); **the WebView candlestick chart renders NOTHING on device (MSFT) → tracked follow-up (Task #7).**

3. **Account-connection wizard — BUILT + on-device (Plaid flow all but done).** `/connect-account` modal + `hooks/useConnect.ts`; Plaid Hosted Link + SnapTrade via `expo-web-browser`. Custom-scheme redirect can't complete in Expo Go → built an **EAS dev-client APK** (`com.brchue.financedashboard`, project `@brchue/finance-dashboard`, id `99316aa0-…`; EAS auth via **`EXPO_TOKEN` in `~/.secrets/expo_token`** — headless browser `eas login` is a dead end, callback hits localhost:34491 on the server). Dev build reached login + Plaid sandbox. Fixed live along the way: (a) **`expo-font@57` hoisted** by `@expo/vector-icons`' `expo-font:"*"` peer → dev build crashed `NoSuchMethodError getDirectConverter` → root `overrides` pin `expo-font ~13.3.2` + rebuild (verified the rebuilt APK's dex has 0 `getDirectConverter`); (b) **versionCode-1 collision** — Android won't update same-versionCode, so a "reinstall" silently kept the old crashing APK; uninstall-first fixes it (consider `autoIncrement`); (c) **hosted-link redirect arrives as `finance-dashboard:///…` (triple slash)** which `openAuthSessionAsync` won't intercept → added deep-link completion **routes** (`app/plaid-hosted-link-complete.tsx`, `app/snaptrade-complete.tsx`) + a module-scoped pending-link_token bridge (`lib/pending-connection.ts`; `take*` guards against double-finalize). **RESOLVED (2026-07-20, commit `be545c3`, local-only) — the 409 was NEVER Layer.** Real root cause: `/api/plaid/hosted-complete` read the `public_token` from `link_sessions[].on_success`, which **Plaid deprecated and no longer populates** for Hosted Link — so sessions that finished "connected" always looked empty. Fix: read the live field `link_sessions[].results.item_add_results[].public_token`, and use the institution name Plaid records for the session (Banks now shows "Tangerine - Personal", not a placeholder). Two supporting fixes in `link/token/create`: (a) **randomize `client_user_id` per session in sandbox only** — a stable id made every retry look like a returning user, which is what actually pulled Plaid's returning-user/Layer path (production keeps the stable per-user id); (b) **removed `hosted_link.is_mobile_app`** — Plaid rejects `linkTokenCreate` with INVALID_FIELD (→ 500) unless a top-level `redirect_uri` is also set, and ours stays unset in sandbox. Verified end-to-end on-device: `POST /api/plaid/hosted-complete 200`, Tangerine accounts populated in Banks. The earlier "is it Layer?" diagnostic logging and the 8×1s `on_success` poll (commit `fadc209`) are gone/superseded. Note: repeat connects legitimately stack (each sandbox connect = a new Plaid Item with new account_ids, so the `plaidAccountId` upsert can't dedup them) — test dupes were cleaned out of `local.db` this session; a same-institution dedup guard is NOT built (would need storing `institution_id`).

**THE auth blocker fixed this session (`c0428e3`) — was breaking ALL sign-in:** `better-auth` floated `^1.0.0`→**1.6.23** (when the SDK53 lockfile was deleted) which requires `drizzle-orm ^0.45.2`, but backend pinned `^0.44.0`→0.44.7; the conflict left `@better-auth/drizzle-adapter` hoisted to root unable to resolve nested `drizzle-orm`. Symptom: `(0, drizzle_orm.eq) is not a function` (webpack) / `Cannot find package drizzle-orm` (externalized). Fix: bump drizzle-orm→`^0.45.2` (one copy hoists to root), pin better-auth→`^1.6.23`, add `serverExternalPackages:[better-auth,@better-auth/expo,@better-auth/drizzle-adapter,drizzle-orm]` to next.config. Verified sign-in 200 + all authed endpoints 200 via session **cookie** (Bearer 401 = no bearer plugin registered — cookie is the `@better-auth/expo` path). Seed: `dev@example.com` / `SEED_PASSWORD` in `backend/.env.local`; data = 5 bank accounts, 0 holdings, 1 standing alert (MSFT>$500), 2 net-worth snapshots.

**⚠ PUSH-STATE CORRECTED 2026-07-20 (verified via `git ls-remote`, not assumed): `origin/main` is at `de8a414` — ONLY the local.db history purge was ever pushed.** Prior notes claiming "all session commits pushed to clean main" were FALSE. Local `main` has NO upstream tracking set, and these **12 commits are committed LOCALLY but UNPUSHED**: `c0428e3` auth-fix, `3377916` app assets, `38370d0` connect wizard, `17b5c2f` dev-client, `a18e3e7`/`05b0493` EAS link+package, `e1e7759` expo-font pin, `91328e0` deep-link routes, `fadc209` (superseded on_success poll), `a380cb3` docs, `be545c3` Plaid Hosted Link fix, `013da6d` this docs correction. Nothing lost — it's all in local history on top of the clean purged base; just push `main` (once, when the user OKs it; set upstream with `-u`) to bring GitHub current. Remote is private (`brchue-ux/finance-dashboard`).
<!-- ▙▄▄ END 2026-07-20 BLOCK ▄▄▟ -->

<!-- ▛▀▀ FRONTEND FEATURES — BUILT + COMMITTED 2026-07-19 ▀▀▜ -->
**All four planned display-only screens are built, headless-verified (tsc clean + `expo export` bundles, grew 1457→1472 modules / 4.6M→5.0M), and committed to live `main` (NOT pushed — never-push flow):**
- **Alerts** (`918ed8d`): re-typed the client from the stale TradingView-only shape to the backend `UnifiedAlert` feed; feed screen + new Manage Alerts screen (`/manage-alerts`: create/pause/re-arm/delete standing price_alerts). Card tap → Holding Detail; "Analyze with Claude" → conversation sheet.
- **Banks tab** (`301eba6`): 5th tab; account cards (masked-balance tap-to-reveal, status pill) from `/api/banks`; card tap → per-account history (`/account/[id]`) reusing TransactionFeed. "+ Add account" is a labeled STUB (launches the paused wizard).
- **Reports** (`d52e1cd`): shared screen via 📊 header icon on Budget+Portfolio; deterministic native mini-charts (net worth, income/expenses, category trends, monthly drill-down via /api/budget, portfolio performance) — no WebView needed.
- **Holding Detail** (`d425215`): native position summary/tax-badge/set-alert/analyze + candlestick chart in `react-native-webview` (lightweight-charts v5 inlined as a bundled static asset `frontend/lib/chart/holding-chart-html.ts`, generated by `frontend/scripts/build-chart-asset.mjs`; OHLCV + cost-basis overlay via postMessage; MA20/MA50 work). Added deps: `react-native-webview@13.13.5`, `lightweight-charts@^5`. **The chart render/bridge CANNOT be verified headlessly — it's in the device handoff.** RSI/MACD (deepentropy) + spec's web-direct-render are tracked follow-ups.
- **Gate is now dependency-readiness (old phrase gate revoked).** These were built under user autonomy to build anything not needing their interactive click-through.

**local.db cleanup — BOTH PARTS DONE (2026-07-20).** `backend/local.db` (real financial data) had been tracked since the initial commit + pushed. **Part 1 (`b25312f`): untracked + gitignored (local.db/*.db/-wal/-shm/-journal/.sqlite) — can never be re-added.** **Part 2 (history purge) DONE:** `git-filter-repo --invert-paths --path backend/local.db --force` + `git push origin main --force` (`197898b`→`de8a414`); `origin/main` tree + history verified clean. On-disk file untouched (gitignored). Only remaining bit: **Windows clone reconcile (user deferred)** — re-clone or `git reset --hard origin/main` + `git gc --prune=now`; NOT `git pull`. GitHub may keep unreachable objects by direct SHA until its GC; repo is private so exposure is limited.
<!-- ▙▄▄ END FRONTEND-FEATURES BLOCK ▄▄▟ -->

<!-- ▛▀▀ SDK53 UPGRADE — DONE 2026-07-19 (npm 11 unblock) ▀▀▜ -->
**SDK 53 alignment is COMPLETE on the live main tree and verified.** Frontend is now dep-coherent at SDK 53 (expo ~53, react 19.0.0, RN 0.79.6, expo-router ~5.1.11, reanimated ~3.17.4, nativewind ~4.1.23). Verified on the live tree (not assumed from the sandbox): **expo-doctor 18/18, tsc clean, real headless Metro bundle succeeds** (`cd frontend && npx expo export --platform android` → 4.6M Hermes bundle, 1457 modules). Committed as `f4b4f47` (with the auth slice); NOT pushed.

- **Root cause was npm 9.2.0's broken workspace hoisting.** Upgraded npm **9.2.0→11.18.0**, user-local (see [[reference-npm-on-ubuntu-homeserver]] in memory): this Ubuntu apt `nodejs` does NOT bundle npm (separate apt `npm` provides 9.2.0), so "Node 22 ships npm 10+" was false here. Installed npm 11 into `~/.local` (`npm config set prefix ~/.local --location=user` — no sudo; `/usr/local` is root-owned).
- **The actual blocker in the tree was the band-aids**, not overrides: a prior session had added `react-native` + `react-native-css-interop` as **root** package.json deps to force hoisting under npm 9. Under npm 11 these caused ERESOLVE (root css-interop's open-ended `reanimated>=3.6.2` peer pulled reanimated 4.5.2 → needs RN 0.83-0.86 ≠ 0.79.6). **Removed the root band-aids** → clean reinstall → single-instance hoisting (reanimated/expo-router/nativewind/css-interop each 1 copy at root). Also the root `@types/react` 18.3.31 override that this doc flagged for removal is GONE (clobbered by the clean root package.json; React 19 made it moot, confirmed by the passing bundle/tsc).
- **Other SDK53 fixes retained** (from the sandbox work, do NOT redo): reanimated pinned `3.17.4`; `@types/react`→19; `typescript`→5.8.3; backend `react`/`react-dom` exact `19.0.0` (single-React coherence); `GradientText.tsx` masked-view React-19 type cast; NativeWind `~4.1.23`; `metro.config.js` monorepo-aware AND now extends Expo's default watchFolders instead of replacing them (fixed the 18/18 doctor check).
- **Apply gotchas (both hit + fixed):** (1) live's first bundle referenced the *worktree's* node_modules — stale shared Metro transform cache; `expo export --clear` fixed it. (2) live's reinstall needed the **stale SDK52 `package-lock.json` deleted** first (it pinned expo-router 4.0.22). (3) esbuild/sharp "allow-scripts" install-script warnings are benign — modern esbuild ships its binary as an optional dep; verified `esbuild.buildSync` works.
- **The `~/projects/home_budget_app-sdk53` worktree (branch `sdk53-sandbox`) is now REDUNDANT** — superseded by live; safe to `git worktree remove` (holds ~1.2G node_modules).

**FRONTEND AUTH SLICE — committed `f4b4f47` (was built on live main), tsc-clean, native path server-verified:**
- `lib/auth.ts` (expoClient + SecureStore on native / localStorage on web), `lib/env.ts` (new, breaks an auth↔api import cycle), `lib/api.ts` (cookie via `getCookie()` on native + `credentials:"include"`; manual `Cookie` header native-only since browsers forbid it on web), `app/(auth)/_layout.tsx` + `app/(tabs)/_layout.tsx` (`useSession()` route guards), `components/llm/ConversationSheet.tsx` (same cookie treatment). Deps added: `better-auth`, `@better-auth/expo`, `expo-web-browser`, `expo-network`, `@react-native-masked-view/masked-view`. (The SDK52-era root `@types/react` 18.3.31 override was removed 2026-07-19 when SDK53 went live — React 19 made it moot.)
- **Verified real (headless):** tsc clean on all auth files; **server cookie contract verified LIVE via curl** (sign-in 200 + `Set-Cookie better-auth.session_token/session_data` → `/api/budget`,`/api/reports`,`/api/auth/get-session` all 200 with cookie → 401 without); CORS preflight from `http://localhost:8081` → 204 + `Allow-Credentials:true`. better-auth client sends `credentials:"include"` on web by default (config.mjs:39). Backend CORS was already correct — no change needed.
- **Two verification handoffs still pending — need a device/browser this HEADLESS homeserver lacks (no adb/emulator/display):** (a) native device tap-through (Expo Go on phone; set `EXPO_PUBLIC_API_URL` to the homeserver LAN IP + :3011, NOT localhost); (b) web browser click-through. Both are user-run.
<!-- ▙▄▄ END NEXT-SESSION BOX ▄▄▟ -->

**Repo:** `~/projects/home_budget_app` (Linux homeserver) ⇄ `C:\Users\bchue\finance-dashboard\` (Windows). GitHub: `brchue-ux/finance-dashboard` (private). Latest pushed commit: **`fa76d24`** ("Backend completion pass"). npm workspaces monorepo (`backend`, `frontend`).

**Gate for building (the old "ignore my previous point" phrase rule is REVOKED — see STANDING RULE below):** build a thing only once everything it depends on is built + working. User's order: **all backend done+verified before any frontend.**

**What is DONE and LIVE-VERIFIED this session** (exercised against a running dev server with real data/accounts, not just typecheck):
- Auth (Better Auth + `@better-auth/expo`), CORS middleware, `/api/portfolio` (credential-leak fix holds), `/api/budget`, `/api/reports`, `/api/system/{status,jobs}`
- LLM advisory chat — real Anthropic v7 streaming; transaction-sign accuracy fixed (inflow/outflow labels + AMOUNT CONVENTION prompt)
- SnapTrade connect (sandbox: user registered, secret stored encrypted, no leak)
- Native alert poller (`yahoo-finance2@3` `quote()` + `marketState` gate), TradingView webhook (per-user hashed secret, no `DEFAULT_USER`), mark-as-read, standing-alert CRUD
- Plaid sandbox sync (enrichment fields, balance snapshots)
- Nightly batch: submit verified after fixing a real bug (`custom_id` colon → Anthropic 400); collection runs via the `batch_poll` cron
- **Banks tab backend** (`/api/banks`, `/api/banks/[id]/transactions`) — built this session, verified
- **CSV import — verified CORRECT on 1,754 real RBC Visa transactions** (right dates, right signs, dedup working). Those rows are in `local.db` (on disk; now untracked+gitignored as of `b25312f` — it had actually been tracked+pushed since the initial commit, contrary to earlier claims; GitHub-history purge still pending, see START-HERE).
- **Google Sheets live import** — OAuth + live read verified end-to-end. NOTE: the user's own "2026 Budget Dashboard" sheet is a *dashboard template, not a transaction ledger*, so it is NOT a usable import source (testing caught it silently mis-parsing year-less dates + expense signs). The importer works; that sheet just isn't the right input.

**The ONE open backend loose end:**
- **Excel/Graph live import — OAuth chain fully verified; full file round-trip blocked on account, not code.** Verified working end-to-end: consent → code exchange → encrypted MSAL token-cache stored → `acquireTokenSilent` returns a valid token → authenticated Graph call reaches Microsoft. The only gap: the account the user authorized with is a **work/org tenant with NO OneDrive** (Graph returns "Tenant does not have a SPO license"), so there were no files to read. To finish the file round-trip: re-authorize with a **personal** Microsoft account (outlook/hotmail/live) that has a OneDrive containing a flat `Date|Description|Amount` `.xlsx` — regenerate consent via `GET /api/import/excel/start` (seed session → follow `Location`), user approves + copies the `code=` from the failed-to-load `localhost:3011` callback, exchange server-side via `exchangeExcelCode`, then `POST /api/import/excel/sync` with `{file, worksheet, mapping}`. Creds in `.env.local` + `~/.secrets/microsoft-oauth.txt`. **Practical note:** if the user has no personal OneDrive, live-Excel is moot for them and the CSV path already ingests Excel exports (verified correct on 1,754 real rows) — so this may simply not need finishing.

**FRONTEND STATUS (updated 2026-07-19 — see the authoritative FRONTEND-FEATURES block at the top):** DONE + committed: auth client slice, Alerts (+ Manage Alerts + unified-feed re-typing), Banks tab (+ per-account history), Reports, Holding Detail (+ WebView chart). PAUSED (needs user OAuth click-through, not built): account-connection UI (Plaid Hosted Link + SnapTrade) + 4-step onboarding wizard — the Banks "+ Add account" is a stub until this lands. FOLLOW-UPS: Holding Detail RSI/MACD (deepentropy) + web-direct chart render.

**How to resume / run the app:**
- Dev server: from `backend/`, `npx next dev --port 3011` (port 3001 is taken by an unrelated container; 3011 is the convention here). Env auto-loads `backend/.env.local`.
- `.env.local` is POPULATED + working for: Anthropic, Plaid sandbox, SnapTrade sandbox, Google OAuth, Microsoft OAuth, auth/seed. Only `ALPHA_VANTAGE_API_KEY` is still empty (OHLCV fallback only — Yahoo primary works). File is gitignored; secrets backed up in `~/.secrets/`.
- Seed login for API testing: `dev@example.com` / password in `~/.secrets/finance-dashboard-local-dev-seed.txt`. Sign in via `POST /api/auth/sign-in/email` to get a session cookie, then hit protected routes.
- Run scripts via the hoisted tsx: `node --env-file=.env.local ../node_modules/.bin/tsx <script>` (workspace hoisting — `backend/node_modules/.bin` is nearly empty).
- **Commits on live `main` not yet pushed (latest pushed remains `fa76d24`):** `f4b4f47` (SDK53 + auth slice), `918ed8d` (Alerts), `301eba6` (Banks), `d52e1cd` (Reports), `d425215` (Holding Detail), `b25312f` (untrack local.db). Working tree clean apart from `local.db` on disk (now gitignored). To run the frontend Metro bundle sanity check: `cd frontend && npx expo export --platform android` (use `--clear` if it references the old sandbox worktree path).

**Authoritative detail lives in:** `spec.md` (what to build), `remediation-decisions-2026-07-18.md` (why every decision), and the "Build status" / "Backend/Frontend Contract Gaps" sections below (kept current).

<!-- ═══════════ END START-HERE BLOCK — historical detail follows ═══════════ -->

A personal finance intelligence dashboard integrating Wealthsimple, bank transactions (RBC + Tangerine + Scotiabank), TradingView, and the Claude API (advisory engine). **Spec status: build-ready at the decision level — remediation decisions folded into `spec.md` on 2026-07-18.** `spec.md` is the buildable source of truth; `remediation-decisions-2026-07-18.md` is the decision-history record (the *why* behind every 2026-07-18 change). The sections below are historical status tracking — trust `spec.md` over them where they differ.

**Culmination pass: backend implemented 2026-07-18, session ended on hitting usage limits before this doc was updated to match.** A follow-up session (2026-07-18, this doc's author) found the working tree far ahead of what this file described — verified for real (typecheck, file-by-file diff reading, then live endpoint exercise against the running server), not assumed. See "Build status" below for the corrected picture. **Frontend is still fully unbuilt.**

**STANDING RULE — UPDATED 2026-07-18 (the old exact-phrase gate is REVOKED):** The previous rule required the exact phrase "ignore my previous point" before building anything. The user explicitly lifted that on 2026-07-18: *"You can ignore my standing rule as of this point on. That is not an explicit blocker."* The **new** gate is a **readiness gate**: before building any component X, confirm that everything X depends on to be reliable and functional is already built and working — if that holds, proceed and build; if not, close those gaps first. The user's stated priority order: **finish ALL backend work (build + verify) before starting any frontend.** Do not treat the old phrase as required anymore, and do not use its absence as a reason to stall — the gate now is dependency-readiness, not a magic phrase.

**Codebase:** synced between `C:\Users\bchue\finance-dashboard\` (Windows) and `~/projects/home_budget_app` (Linux homeserver) via `scripts/sync-*.sh`/`.ps1`.
- `backend/` — Next.js API-only (Railway, port 3001; local dev used 3011 on the homeserver — 3001 is taken by an unrelated container, `home-agent-orchestrator`)
- `frontend/` — Expo React Native (Vercel web + EAS Android APK) — **not yet touched this session, still scaffold-only**

**Architecture locked:** Separated backend + frontend (no monolith). See spec: `wayfinder/spec.md` (in-repo, `.claude/wayfinder/`)

**Build status:**
- Scaffold: complete (all files written)
- **Schema: culmination migration applied 2026-07-18 (first authorized build step, via the exact override phrase).** 22 tables live in local SQLite (`backend/local.db`): +4 new (`job_runs`, `bank_balance_snapshots`, `webhook_credentials`, `ohlcv_cache`), −1 dropped (`import_jobs`, verified empty first), `bank_accounts` +4 balance columns and `connection_id`/`plaid_account_id` now nullable (manual-account support), `transactions` +8 Plaid-enrichment columns, `holdings.open_pnl`, `tradingview_alerts.read_at`. Verified against the real DB: DDL, row counts preserved (4 accounts, 14 transactions, 1 user), `PRAGMA foreign_key_check` clean, indexes present. **Push gotcha for next time:** drizzle-kit's libSQL push breaks when adding columns AND changing nullability on the same table in one pass (its recreate SELECTs new columns from the old table) — split into two pushes; also needs `script -qec "npx drizzle-kit push --force" /dev/null` for the TTY prompt. Turso cloud gets this schema at deploy (spec §10 checklist step 5).
- Prior schema history: original 13 tables, +3 price alerts (`price_alerts`/`alert_fires`/`price_cache`), +4 Better Auth (`user`/`session`/`account`/`verification`, replacing the old minimal `users` table)
- **Auth: verified working end-to-end.** Better Auth's schema was never actually generated before this session — no authenticated route could ever succeed. Fixed via `npx @better-auth/cli generate`; `seed.ts` rewritten to create the seed account through Better Auth's real sign-up path. Confirmed: sign-in returns a session cookie, protected routes correctly 401/200.
- **Plaid Sandbox loop: verified working end-to-end.** link token → sandbox public_token → exchange → sync, confirmed real transaction and account data lands in `local.db`. Fixed a real bug along the way: `link/token/create` was unconditionally sending an unregistered `redirect_uri`, which made Plaid reject `linkTokenCreate` for every institution, not just RBC's OAuth flow (now optional via `PLAID_REDIRECT_URI`). Also added `lib/plaid-accounts.ts` to populate real account names/types from Plaid instead of placeholder values.
- Next.js patched 15.3.3 → 15.5.20 (critical CVE-2025-66478, non-breaking patch bump)
- Turso cloud: token added, dashboard commands run (per earlier session — not verified this session; local dev uses SQLite only, `DATABASE_AUTH_TOKEN` is intentionally empty in `backend/.env.local`)
- Spec: corrected post-completion analysis (2026-07-16) — all critical/high issues resolved; all 6 pre-build items complete as of 2026-07-17. **Spec is build-ready.**
- Item 1 done: MCP server → `twelvedata/mcp` (official, HTTP transport, free tier 800 calls/day)
- Item 2 done: TradingView paid plan friction → native price alert system added (node-cron + yahoo-finance2 quoteCombine; TradingView webhooks now optional enhancement only)
- Item 3 done: LLM merchant categorization — §6 documents uncategorized transactions go to user review queue (not LLM fallback), closing the Ticket 004 inconsistency
- Item 4 done: Tangerine data lag — §5.1 has the UI staleness indicator; §8 System Prompt now has a DATA FRESHNESS section so the LLM doesn't make false-precision claims on stale Tangerine data
- Item 5 done: Prompt cache TTL cost model — §8 has the 5-min TTL note; added a nightly Batch API pre-generation job (§7/§8) at 50% off, synchronous calls reserved for on-demand/alert-triggered sessions
- Item 6 done: Scotiabank fragility — §5.1 known-issue paragraph; §11 has an explicit 4x/year/user relink KPI and a Q3 2027 CDBA migration review date

**Backend/Frontend Contract Gaps (found 2026-07-17) — status of each as of 2026-07-18 (backend now implemented, verified by reading the actual working-tree code, not the decision log):**

Root cause across all of these: backend routes were built and verified in isolation (curl/direct testing), frontend screens were scaffolded against the spec's UI description, and nothing ever cross-checked the two against each other as a single contract.

*Backend implemented (uncommitted in working tree as of 2026-07-18); frontend side of each still pending:*
1. **No Bearer auth plugin** → implemented: `@better-auth/expo` registered server-side (`backend/lib/auth.ts`, `plugins: [expo()]`). Frontend `expoClient()` side not started.
2. **No CORS configuration** → implemented: `backend/middleware.ts`, credentialed explicit-origin allowlist (`CORS_ALLOWED_ORIGINS` env + dev defaults), matcher on `/api/:path*`.
6. **Holding Detail screen doesn't exist** → still not implemented (frontend, untouched this pass).
7. **No account-connection UI at all** → still not implemented (frontend, untouched this pass). Backend Plaid Hosted Link completion route (`app/api/plaid/hosted-complete/route.ts`) is in place waiting for a frontend caller.

3. **Native price alert engine** → implemented: `backend/instrumentation.ts` registers cron via `lib/jobs/scheduler.ts`; `lib/alerts/poller.ts` (201 lines) + `lib/alerts/severity.ts` do the batch `quote()`/`marketState` work per the locked design.
4. **`/api/alerts` wrong table + `DEFAULT_USER` + `alert_fires` FK issue** → implemented: `app/api/alerts/route.ts` returns a real unified `UnifiedAlert[]` feed (native `alert_fires` + `tradingview_alerts`, normalized, sorted, capped at 100), scoped by session `userId` throughout — verified no `DEFAULT_USER` string anywhere in the file. Per-user webhook secrets in `lib/webhook-secrets.ts` + `app/api/settings/webhook-secret/route.ts`.
5. **No mark-as-read endpoint** → implemented: `app/api/alerts/[id]/read/route.ts`.

8. **`/api/portfolio` credential leak** → implemented and verified by reading the file: `wealthsimpleConnections` query now uses an explicit `.select({id, status, lastSyncedAt, createdAt})` projection instead of `.select()`, with a comment citing the contract-gap item. The encrypted SnapTrade token can no longer reach the client via this route.
9. **Env var name mismatch** → applied per spec.md diff (357 lines changed) — not independently re-verified line-by-line.

**Backend implementation for all 9 contract-gap items is done. Frontend is untouched — zero git changes under `frontend/`, no Plaid/SnapTrade deps installed, no Holding Detail screen, no onboarding UI.** Also implemented on the backend: the observability layer (`job_runs` table + `app/api/system/status` + `app/api/system/jobs`), the financial-reporting feature (`bank_balance_snapshots` table + `app/api/reports/route.ts`), LLM tool wiring (`lib/llm/tools.ts` + `advisory.ts` now passes real `tools`/`stopWhen: stepCountIs(8)` to `streamText`/`generateText` — previously confirmed empty), and a CSV import pipeline (`lib/import/csv.ts` + `pipeline.ts` + `app/api/import/csv/route.ts`). AI SDK upgraded `ai@^4.0.0` → `ai@^7.0.31` for real (previously only claimed in docs), with `@ai-sdk/mcp` added.

**Verification done on this backend work (2026-07-18, two passes):** `npx tsc --noEmit` passes with zero errors. Spot-read of `lib/auth.ts`, `app/api/portfolio/route.ts`, `app/api/alerts/route.ts`, `lib/llm/advisory.ts`, `lib/jobs/scheduler.ts`, `lib/llm/tools.ts` confirms the code matches the remediation log's decisions and is genuinely wired together (traced every new module's import graph — `lib/sync/plaid.ts`/`lib/sync/snaptrade.ts` are shared by both the route and the nightly job, not orphaned duplicates; `lib/webhook-secrets.ts` and `lib/jobs/job-runs.ts` are consumed everywhere they should be).

**Runtime-verified for real, not just typechecked** — the `next dev --port 3011` process from the interrupted build session was still running (the homeserver never shut down, only the workstation did); used it directly rather than starting a fresh one:
- Schema migration confirmed live in `local.db` (not just claimed): queried directly, all 22 tables present with correct columns.
- `job_runs` already had 4 real entries from that session (`alert_poll`, `tradingview_webhook`, 2× `import_csv`), all `status: complete` — proof the poller, the webhook handler (using the new per-user hashed secret, not `DEFAULT_USER`), and CSV import had already fired successfully before the session was cut off.
- This session additionally exercised, live, against the seeded user: `/api/system/status`, `/api/system/jobs`, `/api/reports` (verified the net-worth sign logic by hand — credit-card balances correctly subtracted, checking/savings correctly added, matches DB exactly), `/api/alerts`, `/api/portfolio` (confirmed no credential leak — `connection: null`, not the raw row), `/api/budget`, `POST /api/alerts` (standing alert creation, real row inserted), CORS preflight (allowed origin gets echoed `Access-Control-Allow-Origin` + credentials; disallowed origin gets no CORS headers, letting the browser block it — correct per the credentialed-allowlist design), and `POST /api/plaid/sync` (re-verified post-migration: 42 transactions processed, new `plaid_sync` job_run recorded, balances updated). All passed.
- **LLM advisory path — verified live 2026-07-18 (evening, after `ANTHROPIC_API_KEY` supplied).** `POST /api/llm/chat` with a real key streams a coherent, transaction-grounded answer over the v7 UI message stream (`toUIMessageStreamResponse`) — the whole `@ai-sdk/anthropic` v7 + `streamText` + context-assembly chain works end to end. **Content nuance to revisit (not an integration bug):** the model called a `+$500` United Airlines row "spending," but positive amounts are inflows in this app (the reports endpoint correctly counts it as income). That's a context-assembly/system-prompt accuracy gap in how `assembleBudgetContext` presents transaction sign to the model, worth tightening before launch — the pipeline itself is sound.
- **SnapTrade connection — verified live 2026-07-18 (evening, after real sandbox creds supplied).** `POST /api/snaptrade/connect` authenticated against SnapTrade's sandbox with `SNAPTRADE_CLIENT_ID=WE-BUDGET-TINGS-TEST-LOOPD`, registered a user, and returned a valid v4 connection-portal `redirectUri` with the correct `customRedirect` (native deep-link). The user secret persisted **encrypted** (88-char blob, not the raw 50-char key — AES layer confirmed working), and `/api/portfolio` re-checked against this now-connected account returns only `{id, status, lastSyncedAt, createdAt}` — the encrypted token does NOT leak (contract-gap #8 fix holds against a real connection, not just an empty one). **Still not exercised:** an actual portfolio *sync* (`/api/snaptrade/sync`) — requires completing the Wealthsimple OAuth flow in a browser via that redirect URI to link a live brokerage account, which is a manual step.
- **Not yet exercised, remaining:** the nightly 2am job (cron registered correctly; hasn't hit its window — worth a manual invocation, not just waiting), Google Sheets/Excel import (only the CSV path was built and tested — the other two of the three locked import paths are unbuilt), LLM web-search + Twelve Data MCP tools (needs `MCP_SERVICE_URL` pointed at a running Twelve Data MCP instance; web search would work now but wasn't isolated in this test).

*What's confirmed NOT broken, so it isn't re-litigated:* the visual theme (matches `constants/theme.ts` exactly, though the source prototype has since been called "rough/early, not final vision" by the user — see remediation log Ticket 011), the LLM advisory engine's context-assembly half (tool-use half was found broken, see Ticket 010 below), and every *other* frontend hook↔backend route pair not listed above.

**Process fix, still standing:** before marking any future spec or feature "build-ready," run a contract-level pass — enumerate every frontend network call site and every backend route + exported method, and diff them by hand. Backend-only or frontend-only "verified working" is not sufficient evidence the feature works.

**Ticket-Level Re-Evaluation (2026-07-17 — historical record; every open question it raised has since been decided in `remediation-decisions-2026-07-18.md`, read that file for the current answer, not the "recommended direction" text below):**

The contract-gap scan above only checked whether backend and frontend agree at the wire level. A follow-up pass went back through all 12 wayfinder tickets and re-derived each locked decision from first principles — does it serve its own concern well, does it harmonize with what it touches on the other end, does it still serve the project's actual goal — rather than accepting "already decided" or "already built" as justification. `post-completion-analysis.md`'s Part 4 verdict ("none require re-doing any research or re-opening any decisions") does not hold; its own top fix is proof — it corrected the MCP server reference from `tradesdontlie` to `atilaahmettaner/tradingview-mcp`, which was itself later found to be broken (archived core dependency, 403s from cloud IPs) and replaced again with `twelvedata/mcp`. Three answers to one question across this project's history.

*Root cause identified, not just symptoms:* almost every real finding below has the same shape — a ticket decided something that required **both** backend and frontend to agree on a mechanism (not just "use library X"), wrote it as a one-line assertion ("Expo-compatible," "user re-runs OAuth flow," "SSR handled via dynamic import"), and never specified the actual mechanism. Separately, tickets that clearly depended on each other (006 TradingView design vs. 007 tech stack; 011 UI vs. 004 budget scope) never listed each other as blockers, so they were decided in parallel without cross-checking. That's the pattern to watch for on any future ticket, not any single bad call.

*Reaffirmed as-decided* (some with corrected supporting numbers): separated backend/frontend architecture, Railway/Turso/Drizzle/NativeWind; SnapTrade for Wealthsimple (free tier confirmed production-capable via live pricing check — update the cited $ figures, current SnapTrade pricing has changed since research); Plaid for Canadian banks; Yahoo Finance/Polygon.io upgrade path; portfolio chart overlay design; "lossy TradingView webhook delivery is fine" (more justified now that native alerts exist as the load-bearing path); LLM sync-delta trigger, context containerization, card output shape; manual approve/dismiss over `needsApproval` (confirmed inapplicable — that API doesn't exist in the installed SDK version); uncategorized-to-review-queue over LLM-resolves-merchants; seeded-account model; manual AES-256 over `encryptOAuthTokens` (confirmed inapplicable — none of this app's tokens flow through Better Auth's own OAuth layer); theme and UI structure.

*Decisions that were ambiguous or wrong, now with a concrete recommended direction:*
- **Auth mechanism** — should be `@better-auth/expo` (server) + `@better-auth/expo/client` (client, `expoClient({scheme, storage: SecureStore})`), not raw Bearer and not plain cookies. This is Better Auth's own first-party answer for "one backend, Expo web + native," confirmed via current docs.
- **LLM streaming contract** — `backend/package.json` pins `ai@^4.0.0` (actually installed: 4.3.19); spec claims "v7" in four places including a Node-22 deployment checklist step. That upgrade was never done — only documented. Frontend hand-parses the raw v4 wire format with zero `@ai-sdk/react` client. Either commit to v4 explicitly (drop the v7/Node-22 claims until a real upgrade happens) or upgrade both ends together using the SDK's own client stream helpers instead of hand-parsed bytes.
- **Plaid Link integration mechanism** — official native SDK requires a custom EAS dev client (incompatible with Expo Go); never decided or accounted for anywhere. **Locked 2026-07-18, corrected same day:** raw WebView (the original recommendation here) is Plaid's own deprecated integration path. Real mechanism: **Plaid Hosted Link** (`hosted_link_url` from `/link/token/create`, opened via `ASWebAuthenticationSession`/Custom Tabs — exactly what `expo-web-browser`'s `openAuthSessionAsync` wraps) — achieves the same goal (no native SDK, no custom EAS dev client) via Plaid's current sanctioned path instead of a deprecated one. SnapTrade's browser-based hosted-portal flow is unaffected. See `.claude/wayfinder/remediation-decisions-2026-07-18.md` for the full decision record and the still-open two-redirect-URI wrinkle (`completion_redirect_uri` vs `redirect_uri`).
- **SnapTrade connection redirect** — `loginSnapTradeUser()` supports a `customRedirect` param; `/api/snaptrade/connect` never sets it. Without it, a user finishing the Wealthsimple OAuth flow lands on a SnapTrade default page, not back in the app. Same class of bug as the Plaid `redirect_uri` issue already fixed this session.
- **Holding Detail chart rendering on native** — ticket 006's "mobile rendering" decision literally describes Next.js SSR (`dynamic(..., {ssr:false})`), written against a frontend framework this project doesn't use (Expo, not Next.js — decided in a different, non-cross-referenced ticket). Lightweight Charts has no RN renderer; real answer is a `react-native-webview` wrapper with injected-JS data passing. Worth keeping (preserves the deepentropy 446-indicator feature set) but needs to be an explicit, specified decision, not an inherited assumption.
- **Google Sheets historical import** — completely unbuilt (schema only, dead Settings button). Given it's a true one-time, single-user, admin-time action, recommend a `scripts/import-sheets.ts` CLI script (same pattern as `db/seed.ts`) instead of building a third in-app OAuth-redirect flow for something that runs exactly once, ever.

*Genuine new gaps found (beyond the original 9-item contract scan):*
- **LLM tool use is entirely unimplemented.** Spec §8 says web search and the `twelvedata/mcp` server are "enabled." `lib/llm/advisory.ts`'s `generateText`/`streamText` calls pass no `tools:` parameter at all — confirmed by reading the file in full. Claude currently has zero tool access; only static pre-assembled context. `createMCPClient` exists in the installed SDK version, it's just never called.
- **Google Sheets/CSV import is schema-only** (`import_jobs` table, unused) — see recommendation above.

**Remediation walkthrough (2026-07-18) — decisions locked, backend culmination pass implemented, frontend not started:**

`.claude/wayfinder/remediation-decisions-2026-07-18.md` is the authoritative record of a full ticket-by-ticket pass (see file for the complete list: tech stack/auth/streaming, account-connection mechanism + onboarding wizard, chart rendering, LLM tool use, spreadsheet import, UI structure including a new Banks tab and notable-transactions surfacing, and four data-tracking additions found by checking actual vendor API shapes). Every decision in it was re-derived from first principles against real primary sources (vendor docs, installed package type defs, live pricing pages), not assumed or carried over from what was already built. The user authorized the culmination pass ("ignore my previous point") in a 2026-07-18 session; the backend half got implemented (see contract-gap section above) before that session ended on hitting usage limits, which is why this doc wasn't updated until a later session caught the drift. **Nothing from this pass is committed yet — working tree is dirty, HEAD is still `d2a18f3`.**

**Backend-completion pass (2026-07-18, evening) — the phrase-gate was lifted; user directed "finish ALL backend before frontend."** A full completeness audit against `remediation-decisions-2026-07-18.md` confirmed the data-tracking additions (Plaid enrichment fields, `open_pnl`, durable `ohlcv_cache`, `bank_balance_snapshots`), alert management (PATCH/DELETE/standing), Hosted Link, and notable-transactions are all built + wired (earlier nulls were Plaid sandbox sparsity, not gaps). Work done this pass:
- **LLM transaction-sign fix** — `assembleBudgetContext` now labels each transaction `direction: inflow/outflow` + a convention legend, and `prompts.ts` has an AMOUNT CONVENTION block. Verified live: the model now correctly calls the +$500 United Airlines row an inflow (previously called it "spending").
- **Banks tab backend (newly found gap)** — `GET /api/banks` (per-account cards: balance/mask/status/last-synced, manual account handled) and `GET /api/banks/[id]/transactions` (per-account full history, paginated, ownership-scoped). Both verified live. Needed before the Banks frontend.
- **Live Google Sheets + Excel import** — new `spreadsheet_connections` table (encrypted tokens, migrated), shared `normalizeMappedRows` (CSV route refactored onto it, regression-verified), `googleapis` Sheets path (expiry_date refresh-bug guard + token-rotation persistence), MSAL serialized-token-cache Excel/Graph path. Routes: `/api/import/{google,excel}/{start,callback,sync}`. Code built + smoke-tested (503 not-configured, 409 not-connected, 401 unauth, typecheck clean). **Live OAuth flows NOT yet verified — blocked on the user's Google Cloud + Microsoft Entra console setup + credentials** (`GOOGLE_OAUTH_CLIENT_ID/SECRET`, `MS_OAUTH_CLIENT_ID/SECRET/TENANT` in `.env.local`).

**Known open items:**
- Live import OAuth verification — pending the console setup above (the one backend item that needs external OAuth infra only the user can provision).
- `ALPHA_VANTAGE_API_KEY` still empty in `.env.local` (OHLCV fallback only; Yahoo primary works). `ANTHROPIC_API_KEY` + SnapTrade keys are now populated and verified.
- Nightly 2am job: verified via manual trigger (batch submit accepted after the custom_id fix); batch *collection* left to the live `batch_poll` cron.
- Frontend: fully untouched — Bearer-auth client wiring, account-connection UI, onboarding wizard, Holding Detail, Banks tab UI (backend now ready for it). The next major chunk once backend is fully closed out.

**Key env file:** `backend/.env.local` — populated for auth (`ENCRYPTION_KEY`, `BETTER_AUTH_SECRET`, `SEED_EMAIL`/`SEED_PASSWORD` — see `~/.secrets/finance-dashboard-local-dev-seed.txt`) and Plaid Sandbox (`PLAID_CLIENT_ID`/`PLAID_SECRET`, from `~/.secrets/Plaid.txt`). Gitignored, local to each machine — Windows and Linux checkouts need this filled in separately.

**Handoff doc (start here in a new session):**
`~/.claude/projects/C--Users-bchue/wayfinder/handoff-2026-07-16.md`

**Build reminders (deferred items — must surface at build time and post-launch):**
See `~/.claude/projects/C--Users-bchue/wayfinder/build-reminders.md` — contains 8 items that MUST be called out at the relevant build phase or post-launch. Do not begin a build session without checking this file.

See memory file: `finance-dashboard-wayfinder.md`

## Standing Preferences

- Research standard: before any decision is locked, research must cover known bugs, scaling issues, runtime gotchas, hidden advantages, and prior art from publicly available projects. No shallow answers.
- Research subagents run in parallel and are given full time to do deep dives.
- Post-completion analysis: once all wayfinder tickets are resolved, conduct a full analysis of every completed ticket — reviewing the reasoning applied, the research methods used, gaps in coverage, and opportunities missed. This is a mandatory final step before the spec is written.
- Wayfinder tracker: local-markdown (no GitHub repo for this project). Map and tickets live in `~/.claude/projects/C--Users-bchue/wayfinder/`. Research findings live in `wayfinder/research/`.
- Claude Code pinned at v2.1.81 — do not suggest updating (breaks Wispr Flow voice paste on Windows).
