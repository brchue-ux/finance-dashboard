# Handoff — Finance Intelligence Dashboard (Wayfinder)
**Date:** 2026-07-16
**Session focus:** Closed all research tickets, ran post-completion analysis, applied spec corrections, completed 2 of 6 pre-build issue reviews.
**Next session focus:** Continue pre-build issue review — Items 3–6.

---

## Project Overview

Personal finance intelligence dashboard for a single Canadian user. Integrates Wealthsimple (SnapTrade), bank transactions (Plaid — RBC, Tangerine, Scotiabank), TradingView (Lightweight Charts + webhooks), Claude API (advisory engine), and a native price alert system. Android APK + web. Multi-user-ready from day one.

**Codebase:** `C:\Users\bchue\finance-dashboard\`
- `backend/` — Next.js API-only, Railway, port 3001
- `frontend/` — Expo React Native, Vercel web + EAS Android APK

---

## Key Artifacts (read these, don't duplicate)

| Artifact | Path |
|---|---|
| **Spec** (source of truth) | `C:\Users\bchue\.claude\projects\C--Users-bchue\wayfinder\spec.md` |
| **Session compact** (full session summary) | `C:\Users\bchue\.claude\projects\C--Users-bchue\wayfinder\session-2026-07-16-compact.md` |
| **Post-completion analysis** | `C:\Users\bchue\.claude\projects\C--Users-bchue\wayfinder\post-completion-analysis.md` |
| **Build reminders** (deferred items) | `C:\Users\bchue\.claude\projects\C--Users-bchue\wayfinder\build-reminders.md` |
| **Memory file** | `C:\Users\bchue\.claude\projects\C--Users-bchue\memory\finance-dashboard-wayfinder.md` |
| **Price alert architecture research** | `C:\Users\bchue\.claude\projects\C--Users-bchue\wayfinder\research\price-alert-architecture.md` |
| **MCP server evaluation** | `C:\Users\bchue\.claude\projects\C--Users-bchue\wayfinder\research\mcp-tradingview-evaluation.md` |

---

## Where We Are

### Pre-Build Issue Review — 2 of 6 Complete

**✅ Item 1 — MCP server**
`twelvedata/mcp` selected (official vendor-maintained, HTTP transport, free tier 800 calls/day). Replaces the incorrectly referenced `tradesdontlie/tradingview-mcp` (local dev tool only) and rejected `atilaahmettaner/tradingview-mcp` (archived dependency, cloud IP blocked). Spec updated throughout.

**✅ Item 2 — TradingView paid plan friction**
Native price alert system added to spec. In-process `node-cron` poller, `yahoo-finance2` `quoteCombine` as primary data source, 5-minute intervals, market hours aware. TradingView webhooks demoted to optional enhancement for paid-plan users. Three new schema tables added: `price_alerts`, `alert_fires`, `price_cache`. `conditionParams` JSON column ensures forward-compatibility for MA/RSI/volume conditions without schema migration.

---

### ❌ Items 3–6 — Not Started (start here next session)

**Item 3 — LLM unknown merchant categorization**
Ticket 004 decided: "LLM resolves unknown merchants." Spec §6 implements: "dump to uncategorized queue, flag for user review." These contradict. Resolution: async LLM categorization job that runs *after* sync completes (preserves the "LLM never runs on sync" principle from §7). Read spec §6 and §7 before starting. Update spec with the resolved design.

**Item 4 — Tangerine data lag**
Tangerine transactions are 5–9 days stale even on healthy connections. The LLM system prompt (spec §8) has no awareness of this. Two changes needed:
1. Add Tangerine staleness context to the LLM system prompt so it doesn't make false-precision budget recommendations on stale data.
2. Add a user-facing staleness indicator in the Tangerine connection row in Settings.

**Item 5 — Prompt cache TTL cost model**
Anthropic's prompt cache TTL dropped from 60 min to 5 min in early 2026. For a personal finance app with sessions hours apart, the cache is almost always cold. The spec's LLM design (§8) doesn't account for this. Fix: add Batch API nightly analysis (50% discount) for the scheduled 2am analysis run. Synchronous calls remain for on-demand/alert-triggered sessions. This corrects the cost architecture and improves perceived performance (cards pre-generated when user opens the app in the morning).

**Item 6 — Scotiabank fragility**
Plaid's Scotiabank connection is credential/scraping-based with 2–4 expected auth breaks/year. At scale this compounds into support overhead and churn. Two additions to spec needed:
1. Explicit post-launch KPI: log each `ITEM_LOGIN_REQUIRED` event by institution; if Scotiabank relink frequency exceeds 4x/year/user on average, treat as a churn risk signal.
2. CDBA (Consumer-Driven Banking Act) Phase 1 migration: Bill C-15 received Royal Assent March 2026, draft regs June 2026. Phase 1 read access targeted H2 2027. Schedule a Q3 2027 review to migrate Scotiabank (and potentially Tangerine) to a CDBA-compliant aggregator. The `BankSyncProvider` abstraction in the spec is designed to make this a contained swap.

---

## Critical Context for the Next Agent

**The spec is NOT yet ready to build.** Items 3–6 must be resolved first. Do not begin the build phase.

**After items 3–6 are complete:**
- The spec becomes build-ready
- Schema needs `drizzle-kit push` re-run (3 new tables added this session: `price_alerts`, `alert_fires`, `price_cache`)
- First build step: get `npm run dev` working in `backend/`, then Plaid Sandbox core loop

**Key spec facts:**
- Vercel AI SDK v7 is ESM-only — Railway must be configured with `NODE_VERSION=22`
- TradingView webhook timeout is **3 seconds** (hard limit including DNS resolution)
- Railway cost is **~$15–25/mo actual** (not $5/mo)
- Lightweight Charts license is **Apache 2.0** (not MIT)
- `backend/.env.local` has pre-generated `ENCRYPTION_KEY` and `BETTER_AUTH_SECRET` — do not regenerate
- `TWELVE_DATA_API_KEY` is a new required env var (not yet set anywhere)

---

## Deferred Items (tracked in build-reminders.md — do not action now)

- Better Auth `encryptOAuthTokens` decision (build-time: auth setup sprint)
- TradingView paid plan onboarding gate UI (build-time: Alerts tab sprint)
- Twelve Data MCP credit counting verification (build-time: LLM integration sprint)
- Scotiabank CDBA migration (post-launch: Q3 2027 review)
- Scotiabank relink frequency as churn KPI (post-launch: track from day one)
- Tangerine transaction lag user confusion monitoring (post-launch)

---

## Suggested Skills

- **`/grilling`** — stress-test the async LLM categorization design (Item 3) and the Batch API nightly analysis model (Item 5) before locking into spec
- **`/research`** — if Item 5 needs deeper investigation into Batch API patterns
- **`/tdd`** — when building the price alert poller during the build phase
- **`/diagnosing-bugs`** — when `npm run dev` first runs in the backend
- **`/code-review`** — after the first build sprint

---

## Environment Notes
- Platform: Windows 11, shell: bash
- Claude Code pinned at v2.1.81 — do not suggest updating (breaks Wispr Flow voice paste)
- Linux server available at `bchue@192.168.68.62` (SSH key-based auth, confirmed working)
- `L:` drive mounted to `\\sshfs\bchue@192.168.68.62` (persistent SSHFS)
