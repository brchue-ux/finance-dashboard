# Claude Code — Global Instructions

## Active Projects

### Finance Intelligence Dashboard (Wayfinder map)

<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!-- ▛▀▀ SNAPSHOT OF 2026-07-22 — NOT the current state ▀▀▜                 -->
<!-- Current state is owned by `.claude/CLAUDE.md`; this whole file is        -->
<!-- history. Kept verbatim for archaeology; where it disagrees with          -->
<!-- `.claude/CLAUDE.md` or a later dated entry below, THEY WIN.              -->
<!-- Snapshot: 2026-07-22 LATE NIGHT v4 (LATEST: FIVE-BUILD BATCH 1be5f2d — shared Excel sync engine (route/'Sync now'/nightly replay one saved config), item-7 category gate on sync (live-proven: 'Eating Out' unmatched), opt-in nightly auto-resync (auto_sync col, currently OFF), nightly per-view outcomes on Developer screen (self-serve review), formatMoney consolidation + 3001→3011 fallback fix. **READ THE '⚠ DB SAFETY PROTOCOL' SECTION ABOVE START-HERE — two real silent-data-loss incidents this night (drizzle push dropped a table's rows behind '✓ Changes applied'; WAL writes died with a killed dev server), user ordered permanent vigilance.** Earlier v3:  **BACKEND DEPLOYED — systemd user service `wayfinder-backend` on 3011, production standalone build, REAL data, linger enabled; code changes no longer hot-reload: ship via `npm run build && systemctl --user restart wayfinder-backend` (build needs 6GB heap; OOM leaves partial standalone that 404s). HTTPS https://homeserver.tail25a02d.ts.net:7443 via tailscale serve (8443/9443 taken by docker); APP_BASE_URL points there; USER must add that /api/import/excel/callback redirect URI to Entra app b2d0ac8c or new consents fail. Crons now run under systemd. No Railway/Turso — data stays home.** Swipe rewritten on gesture-handler+reanimated (8ae82b7, needs device verdict). Earlier v2:  SCOPED LEARNED RULES built (4dabe42 — account_id/effective_from on learned_rules, rulesForRow at all 4 categorize call sites, WHERE/WHEN chips on the proposal card, local-date fix for evening saves) + IN-APP ONEDRIVE FLOW built (d2b3d82 — oauth_states session-free callback, ?json=1 start, BFS file browser because Graph search lags uploads, /import-excel screen, E2E-verified on real server then verification rows removed; phone-native connect needs only a deployed https redirect URI). 277 tests. NOT device-verified: scope chips + import-excel layout. Earlier: OneDrive/Excel live import PROVEN on real personal OneDrive — /consumers-vs-/common identity-picker trap solved, normalizer hardened for Excel serials/parens-negatives/blank rows (bdc77f2, 272 tests), see RESOLVED Excel bullet; Holding Detail range chips now 1D/1W/1M/3M/6M/1Y/5Y with true intraday bars (a334bd1); DECIDED: rules are SCOPED not catch-all; region/currency = keep-door-open constraint (no hardcoded CAD assumptions in new code). Earlier: ten-build run pushed through 91bf04a7 — see TEN-BUILD RUN bullet in START-HERE; USER'S COURT: approve transfer patterns in Settings, name cash buckets, device-verdict on icons/swipe/alerts/range-chips. Earlier: mega-day — refunds-as-income netting w/ purchase-month reconcile; PRODUCT RULE: one-press nav to anything surfaced; feed day-card redesign = THE reusable dense-list pattern; FIRST LIVE WEALTHSIMPLE CONNECT: item 9 resolved, getUserHoldings 410-retired → positions endpoint, managed-RESP valuation fix ($5.18→$77,841 cent-verified), portfolio account-type groups + managed flag + drill-down + in-app account naming (SnapTrade carries NO WS nicknames); item-10 realistic seed (transfers/coverage/portfolio); LLM cards: length limits + deterministic numeric validation, drop-not-repair. BACKEND IN REAL-DATA MODE — no .env.test, device login dev@example.com. Metro ALWAYS 8082. Earlier same day: ITEM 7 — import category preview/warn, device-confirmed, 0d4c854. Prior: ITEM 6 COMPLETE (6a–6d) + a budget-tab overhaul, DEVICE-CONFIRMED by user and PUSHED as faff7f3. 6c = category proposal from the user's own merchants; 6d = pace-vs-month default + trending-vs-typical on tap. Overhaul: 16 flat cards → 4 parent-GROUP tiles (tap to zoom in-place → category cards → 6d detail); Notable collapsed to one row; Transactions moved off-tab behind one button (app/transactions.tsx); Claude cards un-buried — top card pinned to the tab (trims actionable inline) + "N more" → app/insights.tsx (shared state via useInsights) + badged 💡 header icon; pinned insight & "not in any category" notice are swipe-to-dismiss (SwipeToDismiss, core PanResponder). DEFERRED polish the user named: swipe feel is janky; the 3 header icons + overall theme need work. Prior: 6b learning loop; real multi-account data; transfer/income fix; 6a; import dedup; coverage). -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->

## ⚠ DB SAFETY PROTOCOL — MANDATORY, user-ordered 2026-07-22 ("make note of these errors and the need to be vigilant")

Two silent-data-loss incidents happened in ONE night against the REAL database. Both were survivable only because of backups and paranoid re-checking. These rules are not optional:

1. **`drizzle-kit push` can destroy a table's rows and still print "✓ Changes applied."** It recreated `spreadsheet_connections` WITHOUT copying data (wiping the encrypted MSAL token cache) while adding a defaulted NOT NULL column. **Protocol: before every push, `cp` both DBs to `~/.secrets/local-db-backups/`; after every push, diff row counts across ALL tables against the backup** (5-line python loop over `sqlite_master`) — the tool's success output speaks for schema, never for data.
2. **Direct-sqlite3 writes made near a server kill can vanish with the WAL.** Two deletes verified at the time ("remaining: 0") silently resurrected after the dev→prod server swap — the WAL holding them died with the killed process. **Protocol: direct DB surgery only against a STABLE server (not one about to be killed/swapped); `PRAGMA wal_checkpoint(TRUNCATE)` after writing; re-verify through the RUNNING SERVER'S API, not just a fresh sqlite3 read. Any verification done before a process transition (kill, restart, migration, swap) is STALE — re-verify after.**
3. Corollary already proven twice: a probe that "can't happen" (a dedup reporting duplicates of deleted rows) is evidence, not noise — chase it before building on top.

## 2026-07-27 — Reports "Spent" restored to total outflow (`fm/budget-spent`)

- **The contradiction:** Budget showed `summary.totalOutflow` under "Spent", the Reports monthly drill-down showed `summary.totalSpent` (envelope-attributed spend only). Traced both end to end against real rows: `totalIncome − totalOutflow` equals the net movement of every tracked account to the cent on seed AND real data; `totalSpent` misses by exactly `unattributedSpent` every month (real data ≈ $3,494/month, 31–41% of outflow). Transfers, refunds and splits are handled identically by both, so they do not distinguish the two. Same class as `00f13dd`, which fixed it on the Budget tab; Reports was written later and reached for the figure that had just been removed.
- **Fix (`frontend/app/reports.tsx`):** headline is `totalOutflow`. Swapping the number alone was rejected — `totalSpent` is the sum of the envelope rows rendered directly beneath it, so the headline would stop matching its own list. A tappable "Not in any category" row (`unattributedSpent`, rendered only when > 0) closes the list back to the headline and navigates to `/manage-envelopes`, matching the Budget tab's notice destination per the one-press nav rule.
- **Budget tab, one approved wording change only:** "% of this month's spending" → "% of this month's outflow" (`frontend/app/(tabs)/index.tsx`). The rest of that notice was explicitly out of scope.
- **Regression tests (`backend/lib/budget/summarize.test.ts`, 4 added):** deliberately at backend pure-logic level — the frontend workspace has NO test runner at all (no jest/vitest, no test files, no test script) and root `npm test` runs backend only. They pin the property, not the implementation: the ledger identity, that `income − totalSpent` overstates by the uncategorized spend, that a transfer is in neither figure, and that `totalOutflow` doesn't move when only categorization changes while `totalSpent` does. Verified able to fail by mutating `totalOutflow` to be envelope-only. **Accepted gap:** they do NOT catch a client picking the wrong field, which is the literal regression that happened — frontend test infrastructure is recommended as separate work.
- Investigation touched read-only copies of the DBs only; no product data mutated. **Frontend change NOT device-verified.**
## 2026-07-27 — Expo web target fixes + first frontend test runner (`966f3c1`)

Closed the web-target and UI findings of a prior code review (F3, F4, F7, F8, F9, C4). F5 (Budget vs
Reports disagreeing on "Spent") was explicitly left out of scope as an open product decision, and
`backend/app/api/` was left to a concurrent security worker.

- **F3 — web sign-in 403.** `backend/lib/web-origins.ts` trusted `:8081`/`:19006` while Metro binds
  `:8082`, so Better Auth returned `403 INVALID_ORIGIN` even though the CORS preflight passed.
  `backend/lib/web-origins.test.ts` now *parses* the port out of `frontend/package.json`'s start
  script rather than hardcoding it, so the duplication can't just move one file over.
- **F4 — black screen titles on web.** `@react-native-masked-view` has no real web implementation
  (its `.web.js` renders the mask and discards the children). Fixed at both layers: an explicit
  fallback colour on the mask text, plus a real `GradientText.web.tsx` (`background-image` +
  `background-clip: text` + transparent colour) following the `ChartView.web.tsx` platform split.
  Nine affected screens verified in a real browser. The review's claim that Reports was affected was
  wrong — `app/reports.tsx` uses a plain `Text`.
- **F7 — raw `relink_required` enum shown to the user** on System status. The Banks mapping moved to
  the shared `frontend/lib/connection-status.ts`; Banks' pill strings stay byte-identical (already
  device-verified), System status gets the roomier "Relink needed" prose. Same bug in the
  wealthsimple branch fixed too.
- **F8** — "Last run never run" → "Never run" / "Last run <status>".
- **F9** — clipped "✓ current" marker in the category picker: `flexShrink: 0` on the marker,
  `flex: 1`/`minWidth: 0` on the label, and a right gutter so the last glyph clears the web scrollbar.
- **C4** — 19 tests for `frontend/lib/csv-signs.ts`, the guard against this project's most expensive
  bug (a debit-positive CSV importing 50 purchases as income). One test deliberately *characterises*
  a known `splitCsvLine` wart (it strips a legitimate trailing quote) rather than fixing it — harmless
  here and out of scope. Required a frontend runner: `frontend/vitest.config.ts` (pure, RN-free,
  mirroring backend's) and root `npm test` now runs both workspaces. 286 backend + 19 frontend pass.
- **`AGENTS.md` + root `CLAUDE.md` symlink added**, recording three sharp edges: the primary
  checkout's absolute `DATABASE_URL`, the shared Metro cache resolving `node_modules` against the
  wrong worktree, and the web-origins ↔ `package.json` coupling. `build:web` deliberately does NOT
  get `--clear` (it would slow every build) — documented there instead.

- **Connection status unified onto one table — includes a DELIBERATE, user-visible colour change.**
  `frontend/lib/connection-status.ts` is now the single owner of connection-status label, pill,
  colour and the needs-action predicate, read by all four surfaces: Banks tab, Settings, System
  status, and the tab-bar alert badge (`app/(tabs)/_layout.tsx`). Consequence to be aware of: the
  "Live" dot/label on **Settings and System status** moved from `COLORS.success` (#22C55E) to
  `COLORS.moneyIn` (#34D399), the Banks hue the table was seeded from. **This is intended, not a
  refactor side effect** — `constants/theme.ts` documents #34D399 as "a mint softer than `success`"
  because #22C55E "reads as an alarm-green at list density". Before this, Settings rendered a green
  "● Live" for *any* non-`relink_required` bank, so a `reconnect_required` connection read as
  healthy. The broader UI theme pass remains DEFERRED and untouched; only the connection-status hue
  changed.

All work ran against a disposable scratch DB; the real `local.db` was md5-verified untouched.

## ⇒ START HERE — Finance Dashboard current state (2026-07-21, latest session)

**Phase: `build-reminders.md` item 6 — COMPLETE (6a/6b/6c/6d), device-confirmed by user + PUSHED (`faff7f3`, 2026-07-22).**
- **6c** (`lib/budget/envelope-proposal.ts`, `GET /api/budget/envelopes/proposal`, `app/suggested-categories.tsx`): propose a category set from the user's OWN merchants — recognized clusters (offer only categories they actually spend in) + the merchants no seed rule could place (the gap a shipped taxonomy hides). Real-data check surfaced Walmart/Canadian Tire/BCM Insurance/cannabis/RONA/YMCA as the real gaps.
- **6d** (`lib/budget/pace.ts` → `summarizeEnvelopes` optional `PaceContext` → budget route): default view = **pace against the elapsed month** (EnvelopeCard "Over/On/Under pace" + expected-by-now marker on ProgressBar); tap a card → **trending vs typical** (`EnvelopeDetailSheet`, hotter/cooler than the user's own average; `computeTypicalSpend` excludes the viewed month). Exposed Utilities' $35 target vs $477 typical.
- **Budget-tab overhaul** (same push): 16 flat cards → **4 parent-GROUP tiles** (`group_name` col, `lib/budget/groups.ts` + frontend `lib/groups.ts`, `GroupTile`); tap zooms IN-PLACE (`openGroup` state, no route) → category cards → 6d detail. Group reassignment chips in Manage Categories. **Notable** collapsed to one expandable row. **Transactions** moved off-tab → one button → `app/transactions.tsx` (month nav + full flow). **Claude cards** un-buried: top card **pinned** (`PinnedInsight`, trims Apply/Dismiss inline) + "N more" → `app/insights.tsx`; `useInsights` shares resolved-state (react-query cache) so pin & screen agree; badged 💡 header icon = permanent access. **Swipe-to-dismiss** (`components/ui/SwipeToDismiss.tsx`, core PanResponder+Animated, no gesture-handler dep) on the pinned insight + the "not in any category" notice, so tiles rise to full frame; fallback ✕.
- **DEFERRED polish (user-named, do NOT action unprompted):** (a) swipe feel is janky — smooth it later; (b) the 3 header icons + the overall UI theme need a pass. Plus earlier deferrals: the "Make this a rule?" proposal display/scope; feed readability.
- **ITEM 7 DONE (2026-07-22, device-confirmed + pushed `ede6922` backend / `0d4c854` frontend):** import with a category column previews first (`POST /api/import/csv/preview`, pure `lib/import/category-match.ts` — matched/unmatched with row counts, near-miss suggestions via substring + small synonym table, only against envelopes the user HAS). Clean files commit straight through; mismatches pause on a review step with suggestions pre-filled + per-category picker/keep-as-is; commit takes validated `categoryMappings`, mapped rows get `category_source=manual`. **Bonus fix on ALL import paths (incl. Google/Excel sync): source categories resolve through `resolveCategoryAssignment` to the envelope's own spelling — "groceries" verbatim used to fragment Groceries' totals.** Bare native "Import complete" alert replaced with in-app success state (check, counts, MATCHED FOR YOU recap, View transactions). Shared zod schema `lib/import/csv-request.ts` so preview and commit can't parse differently. 220 backend tests (was 206), mutation-proven. E2E-verified against running server on test.db, every row checked individually; test.db restored to pristine after. Known minor: resolver's 400 message says "envelopes" (shared 6a wording, UI can't trigger it); preview row counts are pre-dedup (documented).
- **REFUNDS-AS-INCOME DONE (2026-07-22, netting DEVICE-CONFIRMED by user; pushed `060bdc5` backend + `2af2a16` frontend):** pure `lib/budget/refunds.ts` `classifyRefunds` over full history — positive envelope-attributed rows: merchant with NO outflow history → income (catches all 140 WS "Interest received" rows, $554, that the INTEREST keyword files under Fees & Interest); exact merchant+amount match ≤90 days prior (each purchase consumed once, nearest-first) → refund effective in the PURCHASE's month — past months reconcile statelessly because summaries recompute on the fly and the budget route already loads full history for 6d; partial/unmatched → refund in landing month. Netting threads through `summarizeEnvelopes`/`summarizeTotals`/`computeTypicalSpend`/reports via optional refund params (defaults preserve old behavior); `saved` unchanged by construction; `totalOutflow − unattributedSpent === totalSpent` identity kept. Feed stays calendar-shaped with a "Refund → Jun" chip; EnvelopeCard shows "$X back" when net-negative (`fmt()` strips sign — real display bug) with progress clamped ≥0. Real data: $3,271.52 = $2,717 refunds + $554 interest. 234 tests, netting mutation-proven. Seed has deterministic fixtures (cross-month AMZN pair + Interest received). **Follow-up in same session (E2E-verified, awaiting device check): one-tap nav** — `/api/budget` AND `/api/banks/:id/transactions` annotate refund rows with `refundMatchedTxnId`/`refundMatchedAccountId`; `TransactionActionSheet` gains "↩ View original purchase" → `/account/:id?highlight=` (reuses the notable-card highlight path). Sheet, not an inline chip — a nested pressable in the feed row is exactly what the old undiscoverable category chip was. Known edge: highlight only scrolls if the purchase is within the account page's first 100 rows.
- **PRODUCT RULE (user, 2026-07-22): anything the app surfaces or names that CAN be navigated to (a notable transaction, a high-spend row, a refund's original purchase) MUST ship with a clean one-press way to get there. Never describe a destination and leave the user to find it. Audit this on every new surfaced-item feature.**
- **FEED REDESIGN (`50e2448`, 2026-07-22, E2E order-verified; user ordered commit+docs after device look = accepted, no explicit "works" quote):** `/api/budget` had NO orderBy — insertion order (merchant-grouped import batches) scrambled the feed's chronology; now `date desc, createdAt desc` AND `TransactionFeed` groups by day internally so the renderer never depends on arrival order. **Day-card pattern (user-chosen from previews) = the app's REUSABLE dense-list pattern — apply it elsewhere later:** inset rounded card per day under a date + day-net header; outflows in quiet `textSecondary`, only money-in colored (`moneyIn` mint — `success` green reads as alarm at list density); muted category identity dots (`CATEGORY_PALETTE`/`categoryColor`, stable hash); days >6 rows collapse behind "Show N more" (auto-expand if the collapse would hide a highlighted row). User: "imply via pleasing colour palettes … we won't completely set the tone for the rest of the app here but we are definitely gonna raise the bar" — this is NOT the full theme pass, which stays deferred.
- **ITEM 9 RESOLVED (2026-07-22, FIRST LIVE WEALTHSIMPLE CONNECT, `27a811f`):** user connected real WS via SnapTrade on device (server flipped to local.db real mode). 24 accounts returned (TFSA/RRSP/FHSA/3×RESP/personal/crypto/12+ `ca_cash_msb` cash — SnapTrade DOES return WS Cash, old open question closed). Tickers are Yahoo-suffixed (`VEQT.TO`) — no mapping needed, `/api/market/ohlcv` prices it (54 bars). REAL bugs found+fixed: `getUserHoldings` retired on our tier (410 "no longer available for your account") → `getUserAccountPositions`; positions-only valuation = $5.18 for a ~$78k relationship because **WS MANAGED accounts (both RESPs ~$19.7k each) itemize NO positions** → totalValue = Σ `account.balance.total` ($77,841.45 verified to the cent), cashValue = Σ real cash ($38,550.80, was hardcoded 0), closed accounts (10/24) skipped, `raw_type` replaces name-sniffing. **CAUTION for later: WS cash is now in portfolio totals; WS chequing TRANSACTIONS also exist on the bank side (CSV import) but have no bank-side balance snapshots — no double-count today, but adding bank balances for WS accounts would double-count net worth.** Backend currently in REAL-DATA mode (no `.env.test`); device login = dev@example.com.
- **2026-07-22 EVENING QUEUE (1→3→4) COMPLETE:** (1) Portfolio tab post-live-connect (`644a55e`,`488e729`,`35690c1`): account-type groups + derived managed flag, invested-vs-cash hero, tap-to-expand drill-down, in-app account naming (SnapTrade carries NO WS nicknames — verified; `account_names` col + PATCH endpoint + rename sheet; FUTURE WATCH: auto-import nicknames if ever exposed). (3) Item-10 seed rebuild (`53f705c`): transfer pairs (real bank strings; test.db had ZERO), coverage row, 30 real-shaped portfolio snapshots (managed RESP/cash/VEQT.TO), no fake connection row; verified through real summarize fns. (4) LLM numeric validation (`2c3d77c`): `lib/llm/validate-cards.ts` — $-grounding, %-relations, $-diff claims; drop-not-repair; both card paths; live-verified dropping 2/5 real cards. Also `c5e5f42`: card length limits (user: "too large and wordy"), bodies 327–443ch→~110ch. transferSource has NO production writer (real transfers were script-marked) — auto-marking on import = candidate feature.
- **TEN-BUILD RUN (2026-07-22 late, all pushed, 261 tests):** validator grounding budget-only (portfolio cites web figures); transfer auto-marking (`transfer_patterns` both DBs, both write paths mark at insert, propose-UI w/ counts — USER approves patterns themselves); net-worth WS double-count guard (`excludeWealthsimpleMirrors`); HeaderIconButton monochrome icon system; server-side highlight-aware paging (page CONTAINING the jump target); generic `DayCardList` → Alerts day-cards (insights deliberately exempt); swipe native-driver springs + momentum exit; success-flash stable fallback host; Plaid same-institution 409 guard (`plaid_institution_id`, not E2E-provable without real link); Holding Detail range chips (live-verified on VEQT.TO). Device-subjective: theme pass + swipe feel await user verdict.
- **REMAINING build:** item 5 region/currency deferred. Test CSV for re-testing: `Transactions/item7-device-test.csv` (gitignored).
- **Metro port is ALWAYS 8082** (8081 = home-automation orchestrator, permanent); baked into `frontend/package.json` start script — stop rediscovering this.

**Split visibility fix (`1e93947`):** a split saved fine but the row looked unchanged (it keeps the parent's `category`, and neither transaction endpoint reported split state). Both `/api/banks/:id/transactions` and `/api/budget` now return `splitCategories` per transaction (page-wide query on the account side; the budget side reuses the splits it already loads); `TransactionFeed` renders "Split · Cat1, Cat2" (Split in the accent colour) instead of the single category. Verified: 4 real split rows in test.db, map output matches what the feed shows.

**⚠️ PROCESS LESSON (2026-07-21, keep): I earlier wrote the UX was "on-device tested" when the user had only confirmed the OLD flow broken — an overclaim. "User said commit/push" is NOT "user confirmed it works." It's now genuinely user-confirmed, but the rule stands: never mark the frontend verified without an explicit user OK.**

**DEFERRED UX POLISH (user feedback, not yet actioned):** (1) the transaction feed readability still needs a polish pass (date-grouping landed, but more to do). (2) The **"Make this a rule?" proposal display** — user is "not crazy about the way it's displayed and all the information," and unsure whether a saved rule should apply to EVERY match or offer **options to choose which** matches/scope. Exploratory ("I don't know") — do NOT redesign unprompted; raise it when the UX-polish pass starts. See [[feedback_word_dump_over_grilling]].

**PUSH-STATE (verified via `git ls-remote` 2026-07-22, refunds session): `origin/main` = `2af2a16` at netting-commit time; the one-tap nav follow-up commits after that — verify with `git ls-remote`.** Refunds commits: `060bdc5` (backend netting), `2af2a16` (frontend chip/display + Metro 8082). Prior: `ede6922`/`0d4c854` (item 7), `faff7f3`/`6d039e5`. **Still verify with `git ls-remote` before trusting any push claim — this file has been wrong about it repeatedly.**

### DEV-SERVER / DEVICE SETUP (learned the hard way this session — don't re-derive)

- **Run Expo from `frontend/`, NOT the repo root.** The root has no `main`/`App.tsx`; starting Expo there makes it use `expo/AppEntry` and red-screen with "Unable to resolve module ../../App". (Metro also auto-writes a stray root `tsconfig.json` — delete it.) Metro's default 8081 is taken by a homeserver Python service, so Expo lands on 8082.
- **The device reaches the API via `frontend/.env.local`** → `EXPO_PUBLIC_API_URL=http://192.168.68.62:3011` (homeserver LAN IP + the backend's port). Without it the app defaults to `http://localhost:3001` = the phone itself = every tab empty. `.env.local` is gitignored (LAN-specific); recreate it if missing. Env only reloads on `expo start` restart.
- **Backend**: `next dev --port 3011` with double `--env-file=.env.local --env-file=.env.test` → serves **test.db** (367 txns, 16 categories, user `demo@test.local`/`test1234`). The real 3,920-txn data is in **local.db** (analysis only; NOT what the device sees).

### REAL DATA now lives in `local.db` — 3,920 transactions across 8 accounts

The app was running on ONE account (RBC Visa CSV) which made credit-card payments look like income. This session ingested the rest. Files live in `Transactions/` (whole folder now gitignored — RBC export carries the full card number in every row). All imported through the real `lib/import` pipeline, not shortcuts; every step backed up to `~/.secrets/local-db-backups/` first.

- **RBC Visa** 1,754 · **Wealthsimple RESP** 997 · **Tangerine Chequing** 521 · **Wealthsimple Chequing** 499 · **Wealthsimple TFSA** 82 · **Tangerine Money-Back Credit Card** 60 · **Wealthsimple Non-registered** 5 · **Wealthsimple FHSA** 2.
- **828 rows flagged as transfers, 980 as out-of-coverage, 2,394 counted in budget totals.**
- Real budget now reads sanely: 2026-06 **$10,615 in / $11,110 out**; 2026-03 is the high month ($19,816 in) — **confirmed a bonus** (single $9,329 STERICYCLE deposit on the employer line, govt deposit was $244), not a tax refund.

### The transfer/income fix (largest thing this session, NOT on the 5–10 list)

The app decided what a transaction *was* from one fact — sign of the amount. Positive = income, negative = spending. On real data that was wrong about MOST of the money: paying a credit card is a positive row on the card and a negative row on chequing (one event, two sides). $90,197 of phantom "income" on the Visa alone; `saved` inherited it and stayed plausible while both halves were wrong. **Fix: a transfer is neither earning nor spending, dropped once at each summarize entry rather than patched into each total.** `lib/budget/transfers.ts`; `withoutTransfers` became `budgetRows` and now excludes for TWO reasons (transfer OR out-of-coverage). Patterns are per-user PROPOSALS, NOT shipped defaults (`SUGGESTED_TRANSFER_PATTERNS`); bare "Bill Payment" deliberately not matched (covers city tax/gas/phone — real spending). `/api/reports` got the same filter or the two screens disagree.

- **`coverage` column**: Wealthsimple history reaches 2021; banks start 2025-05. A 2023 month with only a $40 dividend reads as wildly profitable. Out-of-coverage rows are stored + categorized + visible in their account, just never summed. Per the user: uncorroborated history goes "off to the side."
- **Investment vs cash split**: Wealthsimple's 1,546 trade rows are NEVER imported (1,500 BUYs = −$77,765 would be phantom spending — buying an ETF in a TFSA doesn't leave the household). Only Dividend/Interest/Grant/Fee/Tax count; MoneyMovement rows imported but flagged transfer (bank side already counted). One app account per WS account (RESP ≠ TFSA). Export's last line is a footer ("As of …"), filtered explicitly.
- **STILL income but shouldn't be: refunds** (~$3.1k Amazon/RONA returns). Different, smaller fix — a refund should reduce its envelope, not add to earnings. Noted, not done.

### 6a — per-transaction recategorize (DONE, `1e704b4`)

`PATCH /api/transactions/:id` resolves the submitted name against the user's ACTIVE envelopes and stores the envelope's OWN spelling (`category` holds a name, not an id — "groceries" verbatim would fragment Groceries' totals). **409 on split transactions** (attribution comes from splits; a parent edit moves no money). `category_source` = `manual`/`rule`/NULL protects a hand-set category from a bulk re-derive — the docstring already promised that but it only held on the default path; proven both ways on real data. Frontend: category becomes a tappable chip, row press still opens the split editor. `lib/budget/category-assignment.ts` is the pure resolver.

### Import dedup fix (`a67c004`) — was silently destroying real data

Duplicate detection tested PRESENCE, so three separate $50 transfers on one day were indistinguishable from one transfer imported thrice — kept one, **dropped 74 real rows worth $7,294** on the chequing import. Fixed to compare COUNTS (`duplicateFilter`, pure + tested): re-importing a file stays idempotent, genuine repeats land. File-by-file import is load-bearing: identical rows WITHIN a file are real repeats; identical rows ACROSS files (Money-Back card exports overlapped on one statement) are the same event exported twice — only the file boundary distinguishes them. Imports can target a named account (`ensureManualAccount(userId, name)`) — every CSV used to funnel into one bucket, merging chequing and Visa into one "account" and hiding the transfers between them.

### 6b foundation — merchant normalization (DONE, `3cb061e`)

A learning loop keyed on merchant is worthless if one merchant makes hundreds of keys. `normalizeDescription` now strips per-order ids (`AMZN MKTP CA*097ZX38Y3` — Amazon was 173 merchants) and store codes with a space after `#` (`BOSTON PIZZA # 507`). **Distinct merchants 538 → 365.** The order-id strip REQUIRES A DIGIT in the run: a first attempt ate `BAM*STEM CAMP` (a $1,687 kids' camp) because many processors put the real merchant name after the asterisk — **ONE changed row in 2,335, found only by a row-by-row diff of every transaction; the aggregate looked like a clean win** ([[feedback-aggregates-mask-regressions]]). Re-categorising all 2,335 now produces ZERO changes.

### 6b LEARNING LOOP — DONE this session (4 commits above)

The full loop, built the industry way (we can't infer a merchant's boundary from a population DB we don't have, so the USER declares it — the aggregators' merchant-identity decision, made by the person):

- **Proposal engine** `lib/budget/rule-proposal.ts` (pure, mutation-proven): default proposed pattern = the normalized description (deliberately narrow). Reports `catches` broken down by the envelope each caught row sits in NOW (`byCurrentCategory`) — the real safety check, not a bare count ("9 uncategorized, 3 already in Restaurants — will move"). A test pins the rejected `BILL PAYMENT`-prefix shortcut catching all payees so it can't come back.
- **Matcher precedence** `categorize(desc, envelopes, learnedRules=[])`: learned rules win over ANY seed rule (precedence by SOURCE, not specificity); most-specific-wins WITHIN the learned set. Shared `ruleCatches` predicate so preview and the real matcher can't disagree. Default `[]` = existing callers are byte-for-byte no-ops.
- **Store + durability** `learned_rules` table + `loadCategorizationContext` threads the learned set through ALL THREE categorize paths (import, Plaid sync, recategorize) — without that a later recategorize silently wipes learned results. Drops rules whose envelope was deactivated. Envelope rename cascade extended to `learned_rules.category`.
- **Endpoints** `app/api/budget/learned-rules/`: POST preview (default proposal or live widen/narrow count), POST save (upsert + back-fill history; `refileRowsMatching` — saving and undoing are the same op with a different learned set; manual+split rows never touched), GET list, DELETE :id (removes AND undoes). Verified on a copy of real `local.db`: 316 Tim Hortons rows Restaurants → save → all Groceries → precedence holds → delete → all 316 revert exactly; manual guard exercised (315 refiled).
- **Frontend**: `CategoryPicker` advances to `LearnedRuleProposal` after a real-category correction (widen/narrow box, debounced re-preview, impact summary); `app/manage-learned-rules.tsx` lists/removes rules, linked from the Categories screen.
- **In-app-test UX pass (`69b5fb3`)**: the old way to change a category was a small chip nested inside a tap-to-split row — the two competed for the touch and changing a category was undiscoverable. Replaced with **tap-transaction → `TransactionActionSheet`** (`Change category` / `Split across categories`; split omitted on the blended Budget feed). Change category flows into the 6b proposal, so the loop is reachable. Also **renamed "envelope" → "category" in all VISIBLE text** (titles/buttons/alerts/empty-states); internal identifiers (`Envelope` type, `budget_envelopes` table, `EnvelopeCard`, the `manage-envelopes` route file) intentionally unchanged — not user-facing, big risky refactor for no gain. If a future ask is "rename it everywhere," that's the identifier refactor, separate from this.
- **UX bugs found in the in-app test (fixed, device-verified):** (1) **"Change category picked up but hung, dark screen, no options"** — the REAL cause (after a wrong first guess, below) was that `CategoryPicker`/`LearnedRuleProposal` used a **`flex: 1` root** inside a bottom sheet whose height is content-driven (`maxHeight` only). A `flex:1` child there has no bounded height to fill and **collapses to zero**, so the picker rendered but was 0px tall — just the dark backdrop. Confirmed by contrast with `SplitEditor`, which works because it is content-sized with a bounded `maxHeight` list. Fix: removed `flex:1` from both roots; the category list got its own `maxHeight` so a long one still scrolls. **RN LESSON: never use a `flex:1` root in a content-sized (`maxHeight`-only) bottom sheet — size to content and bound any inner ScrollView.** (1-wrong) My first diagnosis was a "two overlapping Modals" race; I refactored both screens to ONE `Modal` with a `sheet={txn,mode}` state — that did NOT fix it (kept anyway as a cleaner pattern; still don't add a parallel Modal here). Removing a real fix from a wrong theory is how I burned a cycle — compare against the WORKING sibling component next time. (2) **Feed was a wall of text** — every row repeated the date. Fix: `TransactionFeed` groups rows under per-day headers, drops the repeated date (merchant on top, category muted below, amount right). Readability still has a polish pass pending (see DEFERRED UX POLISH up top).
- **`categorization_events` — the durable training set** (`3f7fd9d`): append-only, never updated/deleted. A learned rule is a lossy compression of "this description → this category"; THIS logs the (raw description, normalized merchant handle, user's own category word, previous category, pattern) pair on every manual correction and rule save. It is what a future ML model / entity resolver / aggregator trains on and CANNOT be backfilled. `recordCategorizationEvent` is the one write path; import-time guesses are not labels and are not logged. The precedence architecture already lets a smarter engine slot in UNDER the user's corrections, so nothing learned gets wasted.

Traps that shaped the design (still true): merchant boundary genuinely varies (2 words for Tim Hortons, 4 for "Bill Payment - Cogeco"); do NOT key on first-N-words — `BILL PAYMENT` → 9 payees, `EFT WITHDRAWAL` → hydro AND Wealthsimple, `INTERAC E-TRANSFER` → 10 people. Propose, never impose.

### 6d framing — DECIDED by the user this session

Default view = **pace against the month** (compare spend to how far through the month you are). Tapping an envelope opens the deeper dig = **trending vs typical** (hotter/cooler than the user's own normal). Targets are 17-month averages so half of months are "over" by definition — pass/fail is the wrong default. Build 6d with this two-layer shape.

### SnapTrade / Wealthsimple research (this session)

Free tier = 1 connected user, 5 brokerage connections, real-time, trading included, no contract — this user's exact case, so **$0**. Confirmed: balances, holdings, transactions, read-only. **UNRESOLVED**: every source names Wealthsimple *Trade* only, none mention Wealthsimple *Cash* — the one-minute check is the Broker Support Matrix (client-rendered Notion, won't WebFetch). BUT the user's own WS activities export DOES include a `Chequing` account_type, so the export path already covers cash and the paid connection is less pressing. Sources: snaptrade.com/pricing, /brokerage-integrations/wealthsimple-api, docs.snaptrade.com/docs/faq.

---
### (earlier 2026-07-21 session — historical, kept for context)

**Item 5's region/currency half stays deferred** — single-user Canadian, that is a product trigger not a blocker.

### What this session found and fixed

- **Ghost sessions returned HTTP 200 with data.** `cookieCache` (30d) resolves a session with no DB lookup, so a deleted/restored/swapped-database user stayed logged in. Proven directly: with the user row gone, `/api/auth/get-session` still returned a valid session and `/api/budget` returned 200. `lib/auth-guard.ts` `requireUser()` is now the single seam (was inlined at 33 call sites); it sends `code: USER_NOT_FOUND` so the client can tell this from an ordinary 401 — it could not diagnose it alone, since its cached session also looks valid.
- **`saved` counted uncategorized spend as saved.** Per month on real data, **2026-06 showed +$67 green when it was actually −$228**. `/api/reports` was already correct, so the two screens disagreed about the same month. Totals gained `unattributedSpent` + `totalOutflow`; Budget's "Spent" card now shows outflow, with the remainder surfaced in a tappable notice.
- **`db/seed-test.ts` rebuilt on real data** — real 16-envelope taxonomy, verbatim bank descriptions, `[TEST]` moved OFF `description` (that field is what `categorize()` reads, so tagging it meant the fixture exercised a string production never sends). Each merchant asserts where it must land, checked before any rows are written. Found: **re-seeding was broken outright** (cleanup never deleted `transaction_splits`/`envelope_allocations`, so it died on a FK constraint once the device session created real ones — any table that gains a writer must be added there), and every merchant fired 2–5×/month so a monthly insurance premium was charged five times.
- **`categorize()` now picks the most specific rule, not the first by sort order.** `UBER EATS` vs `UBER` and `CANADIAN TIRE` vs `CANADIAN TIRE GAS` both resolved correctly only by accident of ordering, and `sortOrder` is user-editable. **Re-categorising all 1,762 real transactions produces ZERO changes** — fragility removed without moving real money. The `BELL`/`BELLIES` case that originally motivated this was already handled by token matching, which is why the scope narrowed to "longer rule beats shorter".

**Test suite: 128 → 143.** Every new group was verified able to fail by reverting the fix.

### Device session results (2026-07-21) — all VERIFIED with server-side evidence, not self-report

- **Envelope reallocation works.** Approve → write, with **total budgeted conserved at $2,320** across five moves.
- **Transaction splits work.** Parent `-$84.49`, two parts summing to exactly `-$84.49`. Invariant held.
- **CSV import works** — first time the document picker has ever run. `POST /api/import/csv 200`, 209→259 transactions.
- **Holding Detail chart works** (needed holdings seeded — see below).
- **Rule learned:** a user saying "it worked" is not evidence. Twice, "all worked" meant *navigation* worked while CSV and splits had **zero** server traffic. Check `job_runs`, the request log and row counts before believing a green light — see [[feedback-real-verification]].

### What that session exposed (all fixed)

1. **`$0` allocation masked overspend.** `unconfigured` was `allocated <= 0`, conflating "never set up" with "deliberately $0 this month"; since `overBudget` required `!unconfigured`, a $0 envelope could never report a breach — Shopping hid **$314.63** of spend behind a neutral chip. **A PASSING TEST asserted the wrong behaviour**, so a green suite was protecting the defect.
2. **Notable-card → transaction never landed on the row.** Three compounding causes: the screen fetched only the 30 most recent (older targets absent entirely), `TransactionFeed` separately capped rendering at 30, and nothing ever scrolled.
3. **Native session hijack — the big one.** `/api/import/excel/start` legitimately sets `ms_oauth_state`; the app fetched it through the authenticated client, Android's OkHttp jar captured it, and with `credentials:"include"` the jar **overrode the manual Cookie header**. Every later request carried `cookies=ms_oauth_state len=51` instead of the session (`len=1013`) → 401 → `lib/api.ts` called `signOut()` on ANY 401 → logged out. Fixed with `credentials:"omit"` on native + retry-then-verify before signing out. **Diagnosed with a temporary middleware cookie probe, since guessing had already failed twice.**
4. **CSV import silently inverted every transaction.** All 50 rows positive (debit-positive export) with the negate toggle defaulting off → purchases recorded as income, inflating a month by ~$2.9k. Now blocks on uniform-sign files until the user answers. Logic lives in `frontend/lib/csv-signs.ts`, RN-free so it is testable.
5. **Envelope rename destroyed history** — see the taxonomy block below.

### LLM cost and latency (2026-07-21)

- **Context: 149,803 → 28,674 tokens, $0.4494 → $0.0860 per refresh (81%).** `ROLLUP_THRESHOLD` fired only above 5,000 transactions so never fired at 1,762 — 14 months of raw rows in every request. Now recency-based (3 months raw + monthly rollups). Pretty-printed JSON alone cost 29%.
- **Latency is NOT input-bound — that assumption was wrong and measurement corrected it.** 143.9s with web search vs 30.7s without: the tool loop was ~113s. Budget cards are arithmetic over the user's own transactions and need no web, so `assembleTools(purpose)` gives budget-cards **no tools**; portfolio/chat keep web search + MCP. **28.5s.**
- **Cards are now always served from cache and refreshed behind the response** (32ms cached, 35ms forced, in-flight dedupe, client polls while `refreshing`). Only a true cold start blocks. **The dedupe is module state + post-response work — sound on Railway/`next dev`, NOT on serverless.**

### Real envelope taxonomy applied to `local.db` (2026-07-21)

Backed up first to `~/.secrets/local-db-backups/`. Built **16 envelopes from the household's actual merchants** — added Insurance, Home & Hardware, Personal Care, Fitness & Recreation, Kids & Activities, Cannabis, Travel, Home Services, Fees & Interest. **Coverage 24% → 92% of spend** (925 uncategorized / $72,723 → 212 / $8,116). Walmart → Groceries (170 txns; a Supercentre trip is mostly food, split the big ones later). Targets derived from **17 months of real averages: $5,255/mo**. Regression checks pass — `M.T. BELLIES` → Restaurants (the `BELL` false match did not return); individual rows read per envelope, not just totals.

**⚠ Do NOT ship that 16-envelope list as `DEFAULT_RULES`.** Per the user: *"envelope sets can't be rigid or thrust upon and forced to conform."* It encodes one Ontario household as everyone's default. The reusable part is the **method** — cluster the user's own merchants and propose. See `build-reminders.md` item 6.

**Rename bug found and fixed (`4a5d345`):** `transactions.category` and `transaction_splits.category` store the envelope **name**, and PATCH updated only the envelope — so a rename orphaned all history ($400 → $0, proven before fixing). Both tables now migrate in one `db.transaction()`. Renaming is how a user makes the app fit their categories, so it must not cost them their history.

**Known-and-accepted:** targets from historical averages mean June 2026 shows **9 of 16 envelopes OVER** ($6,933 vs $5,255). Deliberately not "fixed" by inflating targets — that would be shaping data to suit the build. It is a UX question (trending-vs-typical), logged as build-reminders 6d.

### Open items → `build-reminders.md` items 6–10
Per-transaction recategorization + a learning loop (6a/6b); derive envelopes from the user's own merchants (6c); over-budget framing (6d); import category-match warning (7); session valid for 30 days against a deleted/restored user (8); confirm SnapTrade's TSX ticker format (9); rebuild `db/seed-test.ts` around a realistic taxonomy (10). Canada-only merchant rules is item 5.

### Environment right now
`next dev` on **3011 is pointed at `test.db`** (`--env-file=.env.local --env-file=.env.test`). Device login **`demo@test.local` / `test1234`**. Metro on 8082 with `EXPO_PUBLIC_API_URL=http://192.168.68.62:3011`. To go back to real data, restart without `--env-file=.env.test` **and sign out/in** — the 30-day signed cookie cache survives the swap (build-reminders 8). Dev-client APK is **versionCode 2** (set explicitly in `app.config.ts`; `eas.json` `autoIncrement` is rejected with a dynamic `app.config.ts`), so builds now install over each other without an uninstall.

## ⇒ NIGHTLY BATCH WAS SILENTLY HALF-BROKEN — FIXED 2026-07-20 (uncommitted)

`job_runs` showed `nightly_batch` failing 3× then "complete" — but the "complete" run was **`succeeded:1, failed:1`**. Two distinct bugs, both root-caused against the **real stored batches** (Batch API retains results 29 days — `beta.messages.batches.results(id)` replays them, which is how this was diagnosed rather than guessed):

1. **Markdown fences.** Every failing item had `result.type === "succeeded"` at the API level — the failures were our own `JSON.parse`. The model wraps cards in ` ```json … ``` ` **despite `AUTO_CARD_INSTRUCTION` explicitly saying "no markdown fences"** — proof that a prompt instruction is not a parsing contract. Fixed in the new pure `lib/llm/parse-cards.ts`, shared by the batch path *and* `advisory.ts` (which had the identical bare `JSON.parse` — same defect, two call sites).
2. **`max_tokens` truncation.** `MAX_OUTPUT_TOKENS` was 1500; the budget item spent **exactly 1500** output tokens on a `server_tool_use` (web_search) block and ended `stop_reason:"max_tokens"` with **zero text**. Server-tool blocks draw on the same output budget. The portfolio item used 421 and finished because it never searched — which is precisely why it was deterministically 1-of-2. Raised to 16000 in both paths (batch items have no HTTP timeout to stay under).

**The status rule was the reason nobody noticed:** `failed > 0 && succeeded === 0 ? "failed" : "complete"` reported **green whenever a single item succeeded**. Now `failed === 0 ? complete : succeeded === 0 ? failed : "partial"`, with a new **`partial`** status threaded through `JobType`, the schema comment, and **both** frontend readers — `developer.tsx` would otherwise have coloured an unknown status with the *running* colour. (Audit the readers, not just the writer.)

`job_runs.metadata` now carries per-item `{view, outcome, detail}` including `stop_reason`, so a truncation is distinguishable from unparseable output. Previously only `result.type` went to `console.error` and nothing reached disk — there was nothing to diagnose from.

**Verification (real, not assumed):** 11 new tests in `parse-cards.test.ts` built from the actual failing payloads; suite **mutation-proven able to fail** (disabling fence-stripping → 4 failures; dropping the cards-array guard → 2). Replaying both real batches through the new logic: fenced portfolio now yields **4 cards**, budget correctly reports `stop_reason=max_tokens; model returned no text`, and **both batches flip `complete` → `partial`**. The raised ceiling was then proven with a live `generateCards(budget)` call against **`test.db`** — **5 cards in 63s**, the same view that used to truncate. Full suite **126 passing** (was 115), backend + frontend `tsc` clean.

Also closed: **`build-reminders.md` item 1 (`encryptOAuthTokens`) — RESOLVED, not applicable.** Verified against code and DB: `lib/auth.ts` has only `emailAndPassword` + `expo()` (no `socialProviders`), and the `account` table holds only `provider_id:"credential"`. Better Auth never obtains an OAuth token here; all four third-party tokens (Google, Excel/MSAL, Plaid, SnapTrade) go through the app's own `lib/crypto.ts` AES layer. Keep the manual layer; re-open only if a social provider is ever added.

**Note:** run the suite as `npm test` from the repo root or `npx vitest run` from `backend/`. A bare `npx vitest run` at the repo root fails to resolve the `@/` alias in 2 files — pre-existing, confirmed on a clean tree, not a regression.

**A CONTRACT AUDIT (every backend route vs every frontend call site) found the docs were undercounting open work — trust code over the status text below.** It surfaced the single biggest defect in the project:

> **`budget_envelopes` had NO write path anywhere** — no route, no UI, no seed. So `categorize()` looped over zero envelopes and returned `"uncategorized"` for **1,758 of 1,762 real transactions**. `lib/categorization.ts`'s `DEFAULT_RULES` was a dead export despite its own docstring saying it seeds the table. Now fixed: envelope CRUD + defaults + recategorize, and **763 of 1,762 real transactions categorized**.

**Four real categorization bugs were found and fixed against actual transaction descriptions** — `"A&W"` never matched `A & W` (13 rows), `"UBER EATS"` never matched `UBEREATS` (Transport was stealing food delivery), `"STEAM "` never matched `STEAMGAMES.COM`, and `TACO BELL` was claimed by Utilities' `"BELL"` on sort order. **An intermediate punctuation-stripping fix regressed `BELL` → `BELLIES`/`BELLAS`; aggregate counts made that look like an *improvement* and only a case-by-case check caught it.** All are now regression-tested.

**Built this session (all 9 audit items + splits):** notable-transaction cards on Budget; envelope CRUD + `/manage-envelopes`; unconfigured-vs-overBudget state; Settings connection badge; System status + Developer screens; Import UI (`/import`, CSV/Google/Excel); TradingView webhook screen + plan notice (closes the `build-reminders.md` #2 violation); chained connect wizard (`?mode=single` for "+ Add account"); removed dead `/api/plaid/exchange`; **transaction splits** (schema, API, budget math, Reports, LLM context, editor UI).

**ENVELOPE REALLOCATION (added 2026-07-20 late) — the last `TODO` in the codebase is now closed; `grep -rn TODO` over `backend/{lib,app}` and `frontend/{app,components,hooks}` returns nothing.**
Approving an LLM action card used to `console.log` — a visible lie, since `envelope_allocations` was read by `/api/budget` and `lib/llm/context.ts` from day one but **written by nothing**, so per-month overrides could not exist.
- `lib/budget/reallocate.ts` (pure, tested) + `POST /api/budget/allocations/reallocate` — the only write path into the table. Both sides upsert in one `db.transaction()`; the existing `uq_envelope_year_month` index makes it idempotent.
- **`currentAllocation()` is the rule everything must use:** a month's budget is its allocation-row override *if a row exists*, else the standing `monthlyTarget`. Written `override ? override.allocated : target`, **not** `override?.allocated || target` — a deliberate $0 month must not silently fall back to the target. There's a test for exactly this.
- Card `envelope_from`/`envelope_to` are **model-authored names, not ids** — resolved case/whitespace-insensitively against the user's *active* envelopes, and rejected with a UI-showable message if they don't resolve. Overdraw is rejected, never clamped (clamping would move less than the approved amount and break conservation). The card only disappears once the write succeeds.
- Verified live on `test.db`: valid move, hallucinated envelope (400), overdraw (400), self-move across case (400), **a second move on the same pair compounding off 250 not 400**, and 401 unauthenticated. Total budgeted held at 2320 throughout; DB showed 2 allocation rows and zero duplicate `(envelope, year, month)` groups. Tests were mutation-checked twice (loosened negative guard → overdraw test fails; `||` fallback → zero-override test fails).
- **Defect this activated in `lib/llm/context.ts`:** it passed raw allocation rows keyed by `envelopeId` UUID while the `envelopes` payload carried only `name` — **the model could not join them**, and nothing said allocations override `monthlyTarget`. Inert while the table was permanently empty; wrong the instant it became writable. Replaced with a per-envelope `budgetedThisMonth` + a stated `envelopeConvention`; `prompts.ts` now tells the model to copy envelope names exactly and cap `amount` at `budgetedThisMonth`. **This was a decision seam, not a component seam — both halves were individually correct.**

**⚠ UNEXERCISED UI — worth ONE device session covering all of it at once:** split editor (remainder arithmetic, envelope picker, modal), connect-wizard auto-advance, document picker (CSV import), Google/Excel OAuth handoffs, the Holding Detail chart's visual render, and the LLM action-card Approve flow (busy state, per-card error text, flash confirmation — the API beneath it is verified, the taps are not). All are tsc-clean and bundle-clean (5.32 MB) but none have had a real tap.

**Real data status:** `local.db` holds 1,762 real transactions, **0 splits**, and 7 envelopes with **$0 targets** (deliberately — the user does not want to set real budgets yet; the UI now renders that as "unconfigured" rather than a wall of "over budget"). **999 transactions remain uncategorized ($72.7k)** pending envelope decisions only the user can make — dominated by `WAL-MART SUPERCENTER` (171 txns, $17.5k, genuinely ambiguous groceries-vs-shopping).

<!-- ▛▀▀ 2026-07-20 SESSION — SUPERSEDES stale "not pushed / paused / blocked" lines further below ▀▀▜ -->
**All three manual tasks tackled; everything is now on `origin/main` in a clean history.**

1. **local.db GitHub-history purge — DONE.** `git-filter-repo --invert-paths --path backend/local.db --force` (rewrote 22 commits) + `git push origin main --force` (`197898b`→`de8a414`); `origin/main` tree + full history verified clean of any `.db`. The 7 local-only frontend commits shipped in the same clean timeline. On-disk file untouched (gitignored). Repo is PRIVATE. **Windows clone reconcile deferred by user** — re-clone, or `git fetch && git checkout main && git reset --hard origin/main && git gc --prune=now`; do NOT `git pull`.

2. **Device verification — DONE (Android, on-device).** Real blocker chain fixed: ufw firewall (opened `8082` Metro + `3011` backend for `192.168.68.0/22`; 8081 is the home-automation orchestrator, left alone) → Expo Go too new for SDK 53 (current Go is SDK 57; sideloaded the SDK-53 Go APK from `expo.dev/go?sdkVersion=53`) → "remote update request not successful" = missing `frontend/assets/` (generated placeholder PNGs via sharp) + placeholder `eas.projectId` (removed). Screens render well ("impressed, polish later"); the WebView candlestick chart rendered NOTHING on device (MSFT) — **root-caused + fixed in Task #7 (`678ddef`): not a WebView bug but a data bug — Yahoo's trailing still-forming bar had null OHLC that `yahoo.ts` zero-filled, so a 0 candle sat next to ~$500 candles and Lightweight Charts auto-scaled 0..max, crushing all real candles into a top sliver. Fix: drop incomplete bars. User to re-confirm the render on 2026-07-21.**

3. **Account-connection wizard — BUILT + on-device (Plaid flow all but done).** `/connect-account` modal + `hooks/useConnect.ts`; Plaid Hosted Link + SnapTrade via `expo-web-browser`. Custom-scheme redirect can't complete in Expo Go → built an **EAS dev-client APK** (`com.brchue.financedashboard`, project `@brchue/finance-dashboard`, id `99316aa0-…`; EAS auth via **`EXPO_TOKEN` in `~/.secrets/expo_token`** — headless browser `eas login` is a dead end, callback hits localhost:34491 on the server). Dev build reached login + Plaid sandbox. Fixed live along the way: (a) **`expo-font@57` hoisted** by `@expo/vector-icons`' `expo-font:"*"` peer → dev build crashed `NoSuchMethodError getDirectConverter` → root `overrides` pin `expo-font ~13.3.2` + rebuild (verified the rebuilt APK's dex has 0 `getDirectConverter`); (b) **versionCode-1 collision** — Android won't update same-versionCode, so a "reinstall" silently kept the old crashing APK; uninstall-first fixes it (consider `autoIncrement`); (c) **hosted-link redirect arrives as `finance-dashboard:///…` (triple slash)** which `openAuthSessionAsync` won't intercept → added deep-link completion **routes** (`app/plaid-hosted-link-complete.tsx`, `app/snaptrade-complete.tsx`) + a module-scoped pending-link_token bridge (`lib/pending-connection.ts`; `take*` guards against double-finalize). **RESOLVED (2026-07-20, commit `be545c3`, local-only) — the 409 was NEVER Layer.** Real root cause: `/api/plaid/hosted-complete` read the `public_token` from `link_sessions[].on_success`, which **Plaid deprecated and no longer populates** for Hosted Link — so sessions that finished "connected" always looked empty. Fix: read the live field `link_sessions[].results.item_add_results[].public_token`, and use the institution name Plaid records for the session (Banks now shows "Tangerine - Personal", not a placeholder). Two supporting fixes in `link/token/create`: (a) **randomize `client_user_id` per session in sandbox only** — a stable id made every retry look like a returning user, which is what actually pulled Plaid's returning-user/Layer path (production keeps the stable per-user id); (b) **removed `hosted_link.is_mobile_app`** — Plaid rejects `linkTokenCreate` with INVALID_FIELD (→ 500) unless a top-level `redirect_uri` is also set, and ours stays unset in sandbox. Verified end-to-end on-device: `POST /api/plaid/hosted-complete 200`, Tangerine accounts populated in Banks. The earlier "is it Layer?" diagnostic logging and the 8×1s `on_success` poll (commit `fadc209`) are gone/superseded. Note: repeat connects legitimately stack (each sandbox connect = a new Plaid Item with new account_ids, so the `plaidAccountId` upsert can't dedup them) — test dupes were cleaned out of `local.db` this session; a same-institution dedup guard is NOT built (would need storing `institution_id`).

**THE auth blocker fixed this session (`c0428e3`) — was breaking ALL sign-in:** `better-auth` floated `^1.0.0`→**1.6.23** (when the SDK53 lockfile was deleted) which requires `drizzle-orm ^0.45.2`, but backend pinned `^0.44.0`→0.44.7; the conflict left `@better-auth/drizzle-adapter` hoisted to root unable to resolve nested `drizzle-orm`. Symptom: `(0, drizzle_orm.eq) is not a function` (webpack) / `Cannot find package drizzle-orm` (externalized). Fix: bump drizzle-orm→`^0.45.2` (one copy hoists to root), pin better-auth→`^1.6.23`, add `serverExternalPackages:[better-auth,@better-auth/expo,@better-auth/drizzle-adapter,drizzle-orm]` to next.config. Verified sign-in 200 + all authed endpoints 200 via session **cookie** (Bearer 401 = no bearer plugin registered — cookie is the `@better-auth/expo` path). Seed: `dev@example.com` / `SEED_PASSWORD` in `backend/.env.local`; data = 5 bank accounts, 0 holdings, 1 standing alert (MSFT>$500), 2 net-worth snapshots.

**⚠ PUSH-STATE CORRECTED 2026-07-20 (verified via `git ls-remote`, not assumed): `origin/main` is at `de8a414` — ONLY the local.db history purge was ever pushed.** Prior notes claiming "all session commits pushed to clean main" were FALSE. Local `main` has NO upstream tracking set, and these **12 commits are committed LOCALLY but UNPUSHED**: `c0428e3` auth-fix, `3377916` app assets, `38370d0` connect wizard, `17b5c2f` dev-client, `a18e3e7`/`05b0493` EAS link+package, `e1e7759` expo-font pin, `91328e0` deep-link routes, `fadc209` (superseded on_success poll), `a380cb3` docs, `be545c3` Plaid Hosted Link fix, `013da6d` this docs correction. Nothing lost — it's all in local history on top of the clean purged base; just push `main` (once, when the user OKs it; set upstream with `-u`) to bring GitHub current. Remote is private (`brchue-ux/finance-dashboard`).
<!-- ▙▄▄ END 2026-07-20 BLOCK ▄▄▟ -->

<!-- ▛▀▀ FRONTEND FEATURES — BUILT + COMMITTED 2026-07-19 ▀▀▜ -->
**All four planned display-only screens are built, headless-verified (tsc clean + `expo export` bundles, grew 1457→1472 modules / 4.6M→5.0M), and committed to live `main` (NOT pushed — never-push flow):**
- **Alerts** (`918ed8d`): re-typed the client from the stale TradingView-only shape to the backend `UnifiedAlert` feed; feed screen + new Manage Alerts screen (`/manage-alerts`: create/pause/re-arm/delete standing price_alerts). Card tap → Holding Detail; "Analyze with Claude" → conversation sheet.
- **Banks tab** (`301eba6`): 5th tab; account cards (masked-balance tap-to-reveal, status pill) from `/api/banks`; card tap → per-account history (`/account/[id]`) reusing TransactionFeed. "+ Add account" is a labeled STUB (launches the paused wizard).
- **Reports** (`d52e1cd`): shared screen via 📊 header icon on Budget+Portfolio; deterministic native mini-charts (net worth, income/expenses, category trends, monthly drill-down via /api/budget, portfolio performance) — no WebView needed.
- **Holding Detail** (`d425215`, +task#7 `678ddef`/`24cebfb`): native position summary/tax-badge/set-alert/analyze + candlestick chart. Chart logic now lives in ONE shared controller `frontend/lib/chart/holding-chart-controller.js` (plain JS, receives the `LC` namespace) driving both render paths: native inlines it into the WebView asset `frontend/lib/chart/holding-chart-html.ts` (regenerate via `node frontend/scripts/build-chart-asset.mjs`, which wraps the controller in a postMessage bridge), and web resolves `ChartView.web.tsx` to render lightweight-charts straight into a DOM node (no WebView/iframe). Shared prop types in `components/portfolio/chart-types.ts` (avoids a Metro self-import cycle). Indicators: MA20/MA50 overlay price; **RSI(14) + MACD(12,26,9) added as toggleable stacked sub-panes** (chart height grows per active pane). OHLCV is 1y @ **weekly** candles, so all indicators are weekly-period. Deps: `react-native-webview@13.13.5`, `lightweight-charts@^5.2`. **Static verification done (tsc clean; controller math exercised vs real MSFT bars — RSI in 0..100, MACD finite, correct panes/stretch, clean rebuild on toggle-off); the actual on-device/on-web VISUAL render is the user's pending check.**
- **Gate is now dependency-readiness (old phrase gate revoked).** These were built under user autonomy to build anything not needing their interactive click-through.

**local.db cleanup — BOTH PARTS DONE (2026-07-20).** `backend/local.db` (real financial data) had been tracked since the initial commit + pushed. **Part 1 (`b25312f`): untracked + gitignored (local.db/*.db/-wal/-shm/-journal/.sqlite) — can never be re-added.** **Part 2 (history purge) DONE:** `git-filter-repo --invert-paths --path backend/local.db --force` + `git push origin main --force` (`197898b`→`de8a414`); `origin/main` tree + history verified clean. On-disk file untouched (gitignored). Only remaining bit: **Windows clone reconcile (user deferred)** — re-clone or `git reset --hard origin/main` + `git gc --prune=now`; NOT `git pull`. GitHub may keep unreachable objects by direct SHA until its GC; repo is private so exposure is limited.
<!-- ▙▄▄ END FRONTEND-FEATURES BLOCK ▄▄▟ -->

<!-- ▛▀▀ SDK53 UPGRADE — DONE 2026-07-19 (npm 11 unblock) ▀▀▜ -->
**SDK 53 alignment is COMPLETE on the live main tree and verified.** Frontend is now dep-coherent at SDK 53 (expo ~53, react 19.0.0, RN 0.79.6, expo-router ~5.1.11, reanimated ~3.17.4, nativewind ~4.1.23). Verified on the live tree (not assumed from the sandbox): **expo-doctor 18/18, tsc clean, real headless Metro bundle succeeds** (`cd frontend && npx expo export --platform android` → 4.6M Hermes bundle, 1457 modules). Committed as `f4b4f47` (with the auth slice); NOT pushed.

- **Root cause was npm 9.2.0's broken workspace hoisting.** Upgraded npm **9.2.0→11.18.0**, user-local (see [[reference-npm-on-ubuntu-homeserver]] in memory): this Ubuntu apt `nodejs` does NOT bundle npm (separate apt `npm` provides 9.2.0), so "Node 22 ships npm 10+" was false here. Installed npm 11 into `~/.local` (`npm config set prefix ~/.local --location=user` — no sudo; `/usr/local` is root-owned).
- **The actual blocker in the tree was the band-aids**, not overrides: a prior session had added `react-native` + `react-native-css-interop` as **root** package.json deps to force hoisting under npm 9. Under npm 11 these caused ERESOLVE (root css-interop's open-ended `reanimated>=3.6.2` peer pulled reanimated 4.5.2 → needs RN 0.83-0.86 ≠ 0.79.6). **Removed the root band-aids** → clean reinstall → single-instance hoisting (reanimated/expo-router/nativewind/css-interop each 1 copy at root). Also the root `@types/react` 18.3.31 override that this doc flagged for removal is GONE (clobbered by the clean root package.json; React 19 made it moot, confirmed by the passing bundle/tsc).
- **Other SDK53 fixes retained** (from the sandbox work, do NOT redo): reanimated pinned `3.17.4`; `@types/react`→19; `typescript`→5.8.3; backend `react`/`react-dom` exact `19.0.0` (single-React coherence); `GradientText.tsx` masked-view React-19 type cast; NativeWind `~4.1.23`; `metro.config.js` monorepo-aware AND now extends Expo's default watchFolders instead of replacing them (fixed the 18/18 doctor check).
- **Apply gotchas (both hit + fixed):** (1) live's first bundle referenced the *worktree's* node_modules — stale shared Metro transform cache; `expo export --clear` fixed it. (2) live's reinstall needed the **stale SDK52 `package-lock.json` deleted** first (it pinned expo-router 4.0.22). (3) esbuild/sharp "allow-scripts" install-script warnings are benign — modern esbuild ships its binary as an optional dep; verified `esbuild.buildSync` works.
- **The `~/projects/home_budget_app-sdk53` worktree (branch `sdk53-sandbox`) is now REDUNDANT** — superseded by live; safe to `git worktree remove` (holds ~1.2G node_modules).

**FRONTEND AUTH SLICE — committed `f4b4f47` (was built on live main), tsc-clean, native path server-verified:**
- `lib/auth.ts` (expoClient + SecureStore on native / localStorage on web), `lib/env.ts` (new, breaks an auth↔api import cycle), `lib/api.ts` (cookie via `getCookie()` on native + `credentials:"include"`; manual `Cookie` header native-only since browsers forbid it on web), `app/(auth)/_layout.tsx` + `app/(tabs)/_layout.tsx` (`useSession()` route guards), `components/llm/ConversationSheet.tsx` (same cookie treatment). Deps added: `better-auth`, `@better-auth/expo`, `expo-web-browser`, `expo-network`, `@react-native-masked-view/masked-view`. (The SDK52-era root `@types/react` 18.3.31 override was removed 2026-07-19 when SDK53 went live — React 19 made it moot.)
- **Verified real (headless):** tsc clean on all auth files; **server cookie contract verified LIVE via curl** (sign-in 200 + `Set-Cookie better-auth.session_token/session_data` → `/api/budget`,`/api/reports`,`/api/auth/get-session` all 200 with cookie → 401 without); CORS preflight from `http://localhost:8081` → 204 + `Allow-Credentials:true`. better-auth client sends `credentials:"include"` on web by default (config.mjs:39). Backend CORS was already correct — no change needed.
- **Two verification handoffs still pending — need a device/browser this HEADLESS homeserver lacks (no adb/emulator/display):** (a) native device tap-through (Expo Go on phone; set `EXPO_PUBLIC_API_URL` to the homeserver LAN IP + :3011, NOT localhost); (b) web browser click-through. Both are user-run.
<!-- ▙▄▄ END NEXT-SESSION BOX ▄▄▟ -->

**Repo:** `~/projects/home_budget_app` (Linux homeserver) ⇄ `C:\Users\bchue\finance-dashboard\` (Windows). GitHub: `brchue-ux/finance-dashboard` (private). Latest pushed commit: **`fa76d24`** ("Backend completion pass"). npm workspaces monorepo (`backend`, `frontend`).

**Gate for building (the old "ignore my previous point" phrase rule is REVOKED — see STANDING RULE below):** build a thing only once everything it depends on is built + working. User's order: **all backend done+verified before any frontend.**

**What is DONE and LIVE-VERIFIED this session** (exercised against a running dev server with real data/accounts, not just typecheck):
- Auth (Better Auth + `@better-auth/expo`), CORS middleware, `/api/portfolio` (credential-leak fix holds), `/api/budget`, `/api/reports`, `/api/system/{status,jobs}`
- LLM advisory chat — real Anthropic v7 streaming; transaction-sign accuracy fixed (inflow/outflow labels + AMOUNT CONVENTION prompt)
- SnapTrade connect (sandbox: user registered, secret stored encrypted, no leak)
- Native alert poller (`yahoo-finance2@3` `quote()` + `marketState` gate), TradingView webhook (per-user hashed secret, no `DEFAULT_USER`), mark-as-read, standing-alert CRUD
- Plaid sandbox sync (enrichment fields, balance snapshots)
- Nightly batch: submit verified after fixing a real bug (`custom_id` colon → Anthropic 400); collection runs via the `batch_poll` cron
- **Banks tab backend** (`/api/banks`, `/api/banks/[id]/transactions`) — built this session, verified
- **CSV import — verified CORRECT on 1,754 real RBC Visa transactions** (right dates, right signs, dedup working). Those rows are in `local.db` (on disk; now untracked+gitignored as of `b25312f` — it had actually been tracked+pushed since the initial commit, contrary to earlier claims; GitHub-history purge still pending, see START-HERE).
- **Google Sheets live import** — OAuth + live read verified end-to-end. NOTE: the user's own "2026 Budget Dashboard" sheet is a *dashboard template, not a transaction ledger*, so it is NOT a usable import source (testing caught it silently mis-parsing year-less dates + expense signs). The importer works; that sheet just isn't the right input.

**The ONE open backend loose end:**
- **Excel/Graph live import — RESOLVED 2026-07-22 NIGHT: full file round-trip verified on a real personal OneDrive.** The user's `brchue@gmail.com` IS a personal MSA; the earlier "Tenant does not have a SPO license" failure was the **Microsoft identity-picker trap** — `/common` authority signed them into their org-tenant identity (Azure "Default Directory", no OneDrive). Fix that worked: take the `Location` from `GET /api/import/excel/start`, **swap `/common/` → `/consumers/`** in the URL, user signs in via incognito → `client_info` utid `9188040d-…` (consumer tenant) confirms the right identity; `exchangeExcelCode` against `/common` authority happily redeems a consumers-issued code. Verified live: `excelAccessTokenForUser` silent-refresh → Graph `/me/drive` = `driveType:"personal"` → `readExcelUsedRange("Documents/wayfinder-import-test.xlsx","Transactions")` returns the grid → hardened `normalizeMappedRows` handles it (`bdc77f2`: Excel date **serials** — Graph sends "46203.83" not display text; parenthesized accounting negatives; silent blank-row skip; descriptions in error messages). Test workbook stays in the OneDrive `Documents/` for future re-tests (10 engineered rows: 6 import, 3 reject, 1 spacer). Import step itself deliberately NOT run (real-data mode; shared with CSV path). One-off uploads: manual `/consumers` consent URL with `Files.ReadWrite`, redeem via curl + client secret, use once, discard — the stored product grant stays `Files.Read`. **Known gap for the in-app button:** `useConnectExcel` does `api.get` on a 307-redirecting route (would parse Microsoft's HTML as JSON), and the registered `localhost:3011` redirect + session-cookie-less system browser can't complete on-device — in-app connect needs a server-side `state→userId` design + deployed-backend `https` redirect URI before it works from the phone. Tonight's connect used the terminal-orchestrated paste-back flow (same pattern as the Wealthsimple connect).

**FRONTEND STATUS (updated 2026-07-20 — see the authoritative FRONTEND-FEATURES block at the top):** DONE + committed: auth client slice, Alerts (+ Manage Alerts + unified-feed re-typing), Banks tab (+ per-account history), Reports, Holding Detail (chart + RSI/MACD + web-direct render), account-connection wizard (Plaid Hosted Link + SnapTrade — VERIFIED on-device, un-stubs Banks "+ Add account"). The former FOLLOW-UPS (RSI/MACD, web-direct chart render) are now BUILT (task #7, pending the user's visual check). Remaining open ideas (not started, not blocking): daily-candle range selector on Holding Detail; a same-institution Plaid dedup guard (needs storing institution_id).

**AUTOMATED TESTS (added 2026-07-20) — `npm test` from the repo root, or `npx vitest run` in `backend/`.**
Vitest 3.2.7, config at `backend/vitest.config.ts`. **115 tests, all passing.** Scope is deliberately pure logic only — anything touching the DB/Plaid/Anthropic is verified against the running server and `test.db` instead.
- `lib/categorization.test.ts` (26) — every case is a real bug or real transaction description. Includes the four shipped bugs (`A&W`/`A & W`, `UBEREATS`, `STEAMGAMES`, `TACO BELL`) and regression guards that `BELL` must not match `BELLIES`/`BELLAS`.
- `lib/budget/summarize.test.ts` (18) — the money math: unconfigured-vs-overBudget, the exactly-at-target boundary, allocation-overrides-target, notable threshold/cap/ordering.
- `lib/import/{csv,normalize}.test.ts` (16) — RFC-4180 edge cases; sign convention, `negateAmounts`, unparseable-row handling.
- `lib/alerts/severity.test.ts` (11) — both severity paths.
- `lib/budget/reallocate.test.ts` (23) — envelope reallocation: conservation of total budgeted, override-vs-standing-target base, hallucinated/inactive envelope names, overdraw, self-move, the zero-override case.
- **`/api/budget`'s math now lives in `lib/budget/summarize.ts`** (pure, tested) rather than inline in the route. The extraction was verified behaviour-preserving by diffing the live endpoint response before/after — byte-identical.
- The suite was verified to *fail*: reverting the matcher to its pre-fix version fails exactly 3 tests. A green suite that can't fail proves nothing.
- Note: some pure helpers sit in modules that also import `db/index.ts`, which builds a libsql client at import time — hence `env.DATABASE_URL=file::memory:` in the vitest config. No test issues a query. That coupling is a design smell worth unpicking.

**TEST ENVIRONMENT (added 2026-07-20) — use this for any demo/test data.**
`backend/local.db` holds **REAL financial data** (1,700+ real RBC transactions). Do not create test data in it. A separate, isolated test database exists:
- `backend/.env.test` → `DATABASE_URL=file:./test.db` (committed; contains no real secrets). Load it *after* `.env.local`, which supplies the shared auth/encryption secrets.
- Seed it: `node --env-file=.env.local --env-file=.env.test ../node_modules/.bin/tsx db/seed-test.ts`
- Run the app against it: same double `--env-file` on `next dev`.
- `db/seed-test.ts` **refuses to run unless `DATABASE_URL` matches /test/i**, so a forgotten `--env-file` can't write demo rows into `local.db` (guard verified). It is idempotent (clears its own user's rows first) and deterministic (seeded PRNG).
- Every generated row carries a **`[TEST]`** marker in its description/name, so test data is obvious in any screen, log or export.
- **Device-session login: `demo@test.local` / `test1234`.** Deliberately trivial — it is typed by hand on a phone and only ever guards synthetic data. **The seed reads `TEST_SEED_EMAIL` / `TEST_SEED_PASSWORD` / `TEST_SEED_NAME`, NOT the unprefixed `SEED_*` vars** — `.env.local` sets `SEED_EMAIL` and `SEED_PASSWORD` for the *real* dev user, and since this script is run with both env files, reading those would seed `test.db` with the real account's identity and credentials. (This is why the previously documented credential didn't match reality.)
- Contents: demo user `demo@test.local`, 7 envelopes **with real targets set** (unlike local.db, where targets are 0 = unconfigured), 2 accounts, ~206 transactions over 4 months, balance snapshots for the Reports trend, plus a guaranteed-notable transaction per month so those cards always render.
- Categories are assigned by the **real** `categorize()` engine at insert time (mirroring the import/sync path) rather than hardcoded — so a rules regression shows up as uncategorized demo data instead of being masked.

**How to resume / run the app:**
- Dev server: from `backend/`, `npx next dev --port 3011` (port 3001 is taken by an unrelated container; 3011 is the convention here). Env auto-loads `backend/.env.local`.
- `.env.local` is POPULATED + working for: Anthropic, Plaid sandbox, SnapTrade sandbox, Google OAuth, Microsoft OAuth, auth/seed. Only `ALPHA_VANTAGE_API_KEY` is still empty (OHLCV fallback only — Yahoo primary works). File is gitignored; secrets backed up in `~/.secrets/`.
- Seed login for API testing: `dev@example.com` / password in `~/.secrets/finance-dashboard-local-dev-seed.txt`. Sign in via `POST /api/auth/sign-in/email` to get a session cookie, then hit protected routes.
- Run scripts via the hoisted tsx: `node --env-file=.env.local ../node_modules/.bin/tsx <script>` (workspace hoisting — `backend/node_modules/.bin` is nearly empty).
- **Commits on live `main` not yet pushed (latest pushed remains `fa76d24`):** `f4b4f47` (SDK53 + auth slice), `918ed8d` (Alerts), `301eba6` (Banks), `d52e1cd` (Reports), `d425215` (Holding Detail), `b25312f` (untrack local.db). Working tree clean apart from `local.db` on disk (now gitignored). To run the frontend Metro bundle sanity check: `cd frontend && npx expo export --platform android` (use `--clear` if it references the old sandbox worktree path).

**Authoritative detail lives in:** `spec.md` (what to build), `remediation-decisions-2026-07-18.md` (why every decision), and the "Build status" / "Backend/Frontend Contract Gaps" sections below (kept current).

<!-- ═══════════ END START-HERE BLOCK — historical detail follows ═══════════ -->

A personal finance intelligence dashboard integrating Wealthsimple, bank transactions (RBC + Tangerine + Scotiabank), TradingView, and the Claude API (advisory engine). **Spec status: build-ready at the decision level — remediation decisions folded into `spec.md` on 2026-07-18.** `spec.md` is the buildable source of truth; `remediation-decisions-2026-07-18.md` is the decision-history record (the *why* behind every 2026-07-18 change). The sections below are historical status tracking — trust `spec.md` over them where they differ.

**Culmination pass: backend implemented 2026-07-18, session ended on hitting usage limits before this doc was updated to match.** A follow-up session (2026-07-18, this doc's author) found the working tree far ahead of what this file described — verified for real (typecheck, file-by-file diff reading, then live endpoint exercise against the running server), not assumed. See "Build status" below for the corrected picture. **Frontend is still fully unbuilt.**

**STANDING RULE — UPDATED 2026-07-18 (the old exact-phrase gate is REVOKED):** The previous rule required the exact phrase "ignore my previous point" before building anything. The user explicitly lifted that on 2026-07-18: *"You can ignore my standing rule as of this point on. That is not an explicit blocker."* The **new** gate is a **readiness gate**: before building any component X, confirm that everything X depends on to be reliable and functional is already built and working — if that holds, proceed and build; if not, close those gaps first. The user's stated priority order: **finish ALL backend work (build + verify) before starting any frontend.** Do not treat the old phrase as required anymore, and do not use its absence as a reason to stall — the gate now is dependency-readiness, not a magic phrase.

**Codebase:** synced between `C:\Users\bchue\finance-dashboard\` (Windows) and `~/projects/home_budget_app` (Linux homeserver) via `scripts/sync-*.sh`/`.ps1`.
- `backend/` — Next.js API-only (Railway, port 3001; local dev used 3011 on the homeserver — 3001 is taken by an unrelated container, `home-agent-orchestrator`)
- `frontend/` — Expo React Native (Vercel web + EAS Android APK) — **not yet touched this session, still scaffold-only**

**Architecture locked:** Separated backend + frontend (no monolith). See spec: `wayfinder/spec.md` (in-repo, `.claude/wayfinder/`)

**Build status:**
- Scaffold: complete (all files written)
- **Schema: culmination migration applied 2026-07-18 (first authorized build step, via the exact override phrase).** 22 tables live in local SQLite (`backend/local.db`): +4 new (`job_runs`, `bank_balance_snapshots`, `webhook_credentials`, `ohlcv_cache`), −1 dropped (`import_jobs`, verified empty first), `bank_accounts` +4 balance columns and `connection_id`/`plaid_account_id` now nullable (manual-account support), `transactions` +8 Plaid-enrichment columns, `holdings.open_pnl`, `tradingview_alerts.read_at`. Verified against the real DB: DDL, row counts preserved (4 accounts, 14 transactions, 1 user), `PRAGMA foreign_key_check` clean, indexes present. **Push gotcha for next time:** drizzle-kit's libSQL push breaks when adding columns AND changing nullability on the same table in one pass (its recreate SELECTs new columns from the old table) — split into two pushes; also needs `script -qec "npx drizzle-kit push --force" /dev/null` for the TTY prompt. Turso cloud gets this schema at deploy (spec §10 checklist step 5).
- Prior schema history: original 13 tables, +3 price alerts (`price_alerts`/`alert_fires`/`price_cache`), +4 Better Auth (`user`/`session`/`account`/`verification`, replacing the old minimal `users` table)
- **Auth: verified working end-to-end.** Better Auth's schema was never actually generated before this session — no authenticated route could ever succeed. Fixed via `npx @better-auth/cli generate`; `seed.ts` rewritten to create the seed account through Better Auth's real sign-up path. Confirmed: sign-in returns a session cookie, protected routes correctly 401/200.
- **Plaid Sandbox loop: verified working end-to-end.** link token → sandbox public_token → exchange → sync, confirmed real transaction and account data lands in `local.db`. Fixed a real bug along the way: `link/token/create` was unconditionally sending an unregistered `redirect_uri`, which made Plaid reject `linkTokenCreate` for every institution, not just RBC's OAuth flow (now optional via `PLAID_REDIRECT_URI`). Also added `lib/plaid-accounts.ts` to populate real account names/types from Plaid instead of placeholder values.
- Next.js patched 15.3.3 → 15.5.20 (critical CVE-2025-66478, non-breaking patch bump)
- Turso cloud: token added, dashboard commands run (per earlier session — not verified this session; local dev uses SQLite only, `DATABASE_AUTH_TOKEN` is intentionally empty in `backend/.env.local`)
- Spec: corrected post-completion analysis (2026-07-16) — all critical/high issues resolved; all 6 pre-build items complete as of 2026-07-17. **Spec is build-ready.**
- Item 1 done: MCP server → `twelvedata/mcp` (official, HTTP transport, free tier 800 calls/day)
- Item 2 done: TradingView paid plan friction → native price alert system added (node-cron + yahoo-finance2 quoteCombine; TradingView webhooks now optional enhancement only)
- Item 3 done: LLM merchant categorization — §6 documents uncategorized transactions go to user review queue (not LLM fallback), closing the Ticket 004 inconsistency
- Item 4 done: Tangerine data lag — §5.1 has the UI staleness indicator; §8 System Prompt now has a DATA FRESHNESS section so the LLM doesn't make false-precision claims on stale Tangerine data
- Item 5 done: Prompt cache TTL cost model — §8 has the 5-min TTL note; added a nightly Batch API pre-generation job (§7/§8) at 50% off, synchronous calls reserved for on-demand/alert-triggered sessions
- Item 6 done: Scotiabank fragility — §5.1 known-issue paragraph; §11 has an explicit 4x/year/user relink KPI and a Q3 2027 CDBA migration review date

**Backend/Frontend Contract Gaps (found 2026-07-17) — status of each as of 2026-07-18 (backend now implemented, verified by reading the actual working-tree code, not the decision log):**

Root cause across all of these: backend routes were built and verified in isolation (curl/direct testing), frontend screens were scaffolded against the spec's UI description, and nothing ever cross-checked the two against each other as a single contract.

*Backend implemented (uncommitted in working tree as of 2026-07-18); frontend side of each still pending:*
1. **No Bearer auth plugin** → implemented: `@better-auth/expo` registered server-side (`backend/lib/auth.ts`, `plugins: [expo()]`). Frontend `expoClient()` side not started.
2. **No CORS configuration** → implemented: `backend/middleware.ts`, credentialed explicit-origin allowlist (`CORS_ALLOWED_ORIGINS` env + dev defaults), matcher on `/api/:path*`.
6. **Holding Detail screen doesn't exist** → still not implemented (frontend, untouched this pass).
7. **No account-connection UI at all** → still not implemented (frontend, untouched this pass). Backend Plaid Hosted Link completion route (`app/api/plaid/hosted-complete/route.ts`) is in place waiting for a frontend caller.

3. **Native price alert engine** → implemented: `backend/instrumentation.ts` registers cron via `lib/jobs/scheduler.ts`; `lib/alerts/poller.ts` (201 lines) + `lib/alerts/severity.ts` do the batch `quote()`/`marketState` work per the locked design.
4. **`/api/alerts` wrong table + `DEFAULT_USER` + `alert_fires` FK issue** → implemented: `app/api/alerts/route.ts` returns a real unified `UnifiedAlert[]` feed (native `alert_fires` + `tradingview_alerts`, normalized, sorted, capped at 100), scoped by session `userId` throughout — verified no `DEFAULT_USER` string anywhere in the file. Per-user webhook secrets in `lib/webhook-secrets.ts` + `app/api/settings/webhook-secret/route.ts`.
5. **No mark-as-read endpoint** → implemented: `app/api/alerts/[id]/read/route.ts`.

8. **`/api/portfolio` credential leak** → implemented and verified by reading the file: `wealthsimpleConnections` query now uses an explicit `.select({id, status, lastSyncedAt, createdAt})` projection instead of `.select()`, with a comment citing the contract-gap item. The encrypted SnapTrade token can no longer reach the client via this route.
9. **Env var name mismatch** → applied per spec.md diff (357 lines changed) — not independently re-verified line-by-line.

**Backend implementation for all 9 contract-gap items is done. Frontend is untouched — zero git changes under `frontend/`, no Plaid/SnapTrade deps installed, no Holding Detail screen, no onboarding UI.** Also implemented on the backend: the observability layer (`job_runs` table + `app/api/system/status` + `app/api/system/jobs`), the financial-reporting feature (`bank_balance_snapshots` table + `app/api/reports/route.ts`), LLM tool wiring (`lib/llm/tools.ts` + `advisory.ts` now passes real `tools`/`stopWhen: stepCountIs(8)` to `streamText`/`generateText` — previously confirmed empty), and a CSV import pipeline (`lib/import/csv.ts` + `pipeline.ts` + `app/api/import/csv/route.ts`). AI SDK upgraded `ai@^4.0.0` → `ai@^7.0.31` for real (previously only claimed in docs), with `@ai-sdk/mcp` added.

**Verification done on this backend work (2026-07-18, two passes):** `npx tsc --noEmit` passes with zero errors. Spot-read of `lib/auth.ts`, `app/api/portfolio/route.ts`, `app/api/alerts/route.ts`, `lib/llm/advisory.ts`, `lib/jobs/scheduler.ts`, `lib/llm/tools.ts` confirms the code matches the remediation log's decisions and is genuinely wired together (traced every new module's import graph — `lib/sync/plaid.ts`/`lib/sync/snaptrade.ts` are shared by both the route and the nightly job, not orphaned duplicates; `lib/webhook-secrets.ts` and `lib/jobs/job-runs.ts` are consumed everywhere they should be).

**Runtime-verified for real, not just typechecked** — the `next dev --port 3011` process from the interrupted build session was still running (the homeserver never shut down, only the workstation did); used it directly rather than starting a fresh one:
- Schema migration confirmed live in `local.db` (not just claimed): queried directly, all 22 tables present with correct columns.
- `job_runs` already had 4 real entries from that session (`alert_poll`, `tradingview_webhook`, 2× `import_csv`), all `status: complete` — proof the poller, the webhook handler (using the new per-user hashed secret, not `DEFAULT_USER`), and CSV import had already fired successfully before the session was cut off.
- This session additionally exercised, live, against the seeded user: `/api/system/status`, `/api/system/jobs`, `/api/reports` (verified the net-worth sign logic by hand — credit-card balances correctly subtracted, checking/savings correctly added, matches DB exactly), `/api/alerts`, `/api/portfolio` (confirmed no credential leak — `connection: null`, not the raw row), `/api/budget`, `POST /api/alerts` (standing alert creation, real row inserted), CORS preflight (allowed origin gets echoed `Access-Control-Allow-Origin` + credentials; disallowed origin gets no CORS headers, letting the browser block it — correct per the credentialed-allowlist design), and `POST /api/plaid/sync` (re-verified post-migration: 42 transactions processed, new `plaid_sync` job_run recorded, balances updated). All passed.
- **LLM advisory path — verified live 2026-07-18 (evening, after `ANTHROPIC_API_KEY` supplied).** `POST /api/llm/chat` with a real key streams a coherent, transaction-grounded answer over the v7 UI message stream (`toUIMessageStreamResponse`) — the whole `@ai-sdk/anthropic` v7 + `streamText` + context-assembly chain works end to end. **Content nuance to revisit (not an integration bug):** the model called a `+$500` United Airlines row "spending," but positive amounts are inflows in this app (the reports endpoint correctly counts it as income). That's a context-assembly/system-prompt accuracy gap in how `assembleBudgetContext` presents transaction sign to the model, worth tightening before launch — the pipeline itself is sound.
- **SnapTrade connection — verified live 2026-07-18 (evening, after real sandbox creds supplied).** `POST /api/snaptrade/connect` authenticated against SnapTrade's sandbox with `SNAPTRADE_CLIENT_ID=WE-BUDGET-TINGS-TEST-LOOPD`, registered a user, and returned a valid v4 connection-portal `redirectUri` with the correct `customRedirect` (native deep-link). The user secret persisted **encrypted** (88-char blob, not the raw 50-char key — AES layer confirmed working), and `/api/portfolio` re-checked against this now-connected account returns only `{id, status, lastSyncedAt, createdAt}` — the encrypted token does NOT leak (contract-gap #8 fix holds against a real connection, not just an empty one). **Still not exercised:** an actual portfolio *sync* (`/api/snaptrade/sync`) — requires completing the Wealthsimple OAuth flow in a browser via that redirect URI to link a live brokerage account, which is a manual step.
- **Not yet exercised, remaining:** the nightly 2am job (cron registered correctly; hasn't hit its window — worth a manual invocation, not just waiting), Google Sheets/Excel import (only the CSV path was built and tested — the other two of the three locked import paths are unbuilt), LLM web-search + Twelve Data MCP tools (needs `MCP_SERVICE_URL` pointed at a running Twelve Data MCP instance; web search would work now but wasn't isolated in this test).

*What's confirmed NOT broken, so it isn't re-litigated:* the visual theme (matches `constants/theme.ts` exactly, though the source prototype has since been called "rough/early, not final vision" by the user — see remediation log Ticket 011), the LLM advisory engine's context-assembly half (tool-use half was found broken, see Ticket 010 below), and every *other* frontend hook↔backend route pair not listed above.

**Process fix, still standing:** before marking any future spec or feature "build-ready," run a contract-level pass — enumerate every frontend network call site and every backend route + exported method, and diff them by hand. Backend-only or frontend-only "verified working" is not sufficient evidence the feature works.

**Ticket-Level Re-Evaluation (2026-07-17 — historical record; every open question it raised has since been decided in `remediation-decisions-2026-07-18.md`, read that file for the current answer, not the "recommended direction" text below):**

The contract-gap scan above only checked whether backend and frontend agree at the wire level. A follow-up pass went back through all 12 wayfinder tickets and re-derived each locked decision from first principles — does it serve its own concern well, does it harmonize with what it touches on the other end, does it still serve the project's actual goal — rather than accepting "already decided" or "already built" as justification. `post-completion-analysis.md`'s Part 4 verdict ("none require re-doing any research or re-opening any decisions") does not hold; its own top fix is proof — it corrected the MCP server reference from `tradesdontlie` to `atilaahmettaner/tradingview-mcp`, which was itself later found to be broken (archived core dependency, 403s from cloud IPs) and replaced again with `twelvedata/mcp`. Three answers to one question across this project's history.

*Root cause identified, not just symptoms:* almost every real finding below has the same shape — a ticket decided something that required **both** backend and frontend to agree on a mechanism (not just "use library X"), wrote it as a one-line assertion ("Expo-compatible," "user re-runs OAuth flow," "SSR handled via dynamic import"), and never specified the actual mechanism. Separately, tickets that clearly depended on each other (006 TradingView design vs. 007 tech stack; 011 UI vs. 004 budget scope) never listed each other as blockers, so they were decided in parallel without cross-checking. That's the pattern to watch for on any future ticket, not any single bad call.

*Reaffirmed as-decided* (some with corrected supporting numbers): separated backend/frontend architecture, Railway/Turso/Drizzle/NativeWind; SnapTrade for Wealthsimple (free tier confirmed production-capable via live pricing check — update the cited $ figures, current SnapTrade pricing has changed since research); Plaid for Canadian banks; Yahoo Finance/Polygon.io upgrade path; portfolio chart overlay design; "lossy TradingView webhook delivery is fine" (more justified now that native alerts exist as the load-bearing path); LLM sync-delta trigger, context containerization, card output shape; manual approve/dismiss over `needsApproval` (confirmed inapplicable — that API doesn't exist in the installed SDK version); uncategorized-to-review-queue over LLM-resolves-merchants; seeded-account model; manual AES-256 over `encryptOAuthTokens` (confirmed inapplicable — none of this app's tokens flow through Better Auth's own OAuth layer); theme and UI structure.

*Decisions that were ambiguous or wrong, now with a concrete recommended direction:*
- **Auth mechanism** — should be `@better-auth/expo` (server) + `@better-auth/expo/client` (client, `expoClient({scheme, storage: SecureStore})`), not raw Bearer and not plain cookies. This is Better Auth's own first-party answer for "one backend, Expo web + native," confirmed via current docs.
- **LLM streaming contract** — `backend/package.json` pins `ai@^4.0.0` (actually installed: 4.3.19); spec claims "v7" in four places including a Node-22 deployment checklist step. That upgrade was never done — only documented. Frontend hand-parses the raw v4 wire format with zero `@ai-sdk/react` client. Either commit to v4 explicitly (drop the v7/Node-22 claims until a real upgrade happens) or upgrade both ends together using the SDK's own client stream helpers instead of hand-parsed bytes.
- **Plaid Link integration mechanism** — official native SDK requires a custom EAS dev client (incompatible with Expo Go); never decided or accounted for anywhere. **Locked 2026-07-18, corrected same day:** raw WebView (the original recommendation here) is Plaid's own deprecated integration path. Real mechanism: **Plaid Hosted Link** (`hosted_link_url` from `/link/token/create`, opened via `ASWebAuthenticationSession`/Custom Tabs — exactly what `expo-web-browser`'s `openAuthSessionAsync` wraps) — achieves the same goal (no native SDK, no custom EAS dev client) via Plaid's current sanctioned path instead of a deprecated one. SnapTrade's browser-based hosted-portal flow is unaffected. See `.claude/wayfinder/remediation-decisions-2026-07-18.md` for the full decision record and the still-open two-redirect-URI wrinkle (`completion_redirect_uri` vs `redirect_uri`).
- **SnapTrade connection redirect** — `loginSnapTradeUser()` supports a `customRedirect` param; `/api/snaptrade/connect` never sets it. Without it, a user finishing the Wealthsimple OAuth flow lands on a SnapTrade default page, not back in the app. Same class of bug as the Plaid `redirect_uri` issue already fixed this session.
- **Holding Detail chart rendering on native** — ticket 006's "mobile rendering" decision literally describes Next.js SSR (`dynamic(..., {ssr:false})`), written against a frontend framework this project doesn't use (Expo, not Next.js — decided in a different, non-cross-referenced ticket). Lightweight Charts has no RN renderer; real answer is a `react-native-webview` wrapper with injected-JS data passing. Worth keeping (preserves the deepentropy 446-indicator feature set) but needs to be an explicit, specified decision, not an inherited assumption.
- **Google Sheets historical import** — completely unbuilt (schema only, dead Settings button). Given it's a true one-time, single-user, admin-time action, recommend a `scripts/import-sheets.ts` CLI script (same pattern as `db/seed.ts`) instead of building a third in-app OAuth-redirect flow for something that runs exactly once, ever.

*Genuine new gaps found (beyond the original 9-item contract scan):*
- **LLM tool use is entirely unimplemented.** Spec §8 says web search and the `twelvedata/mcp` server are "enabled." `lib/llm/advisory.ts`'s `generateText`/`streamText` calls pass no `tools:` parameter at all — confirmed by reading the file in full. Claude currently has zero tool access; only static pre-assembled context. `createMCPClient` exists in the installed SDK version, it's just never called.
- **Google Sheets/CSV import is schema-only** (`import_jobs` table, unused) — see recommendation above.

**Remediation walkthrough (2026-07-18) — decisions locked, backend culmination pass implemented, frontend not started:**

`.claude/wayfinder/remediation-decisions-2026-07-18.md` is the authoritative record of a full ticket-by-ticket pass (see file for the complete list: tech stack/auth/streaming, account-connection mechanism + onboarding wizard, chart rendering, LLM tool use, spreadsheet import, UI structure including a new Banks tab and notable-transactions surfacing, and four data-tracking additions found by checking actual vendor API shapes). Every decision in it was re-derived from first principles against real primary sources (vendor docs, installed package type defs, live pricing pages), not assumed or carried over from what was already built. The user authorized the culmination pass ("ignore my previous point") in a 2026-07-18 session; the backend half got implemented (see contract-gap section above) before that session ended on hitting usage limits, which is why this doc wasn't updated until a later session caught the drift. **Nothing from this pass is committed yet — working tree is dirty, HEAD is still `d2a18f3`.**

**Backend-completion pass (2026-07-18, evening) — the phrase-gate was lifted; user directed "finish ALL backend before frontend."** A full completeness audit against `remediation-decisions-2026-07-18.md` confirmed the data-tracking additions (Plaid enrichment fields, `open_pnl`, durable `ohlcv_cache`, `bank_balance_snapshots`), alert management (PATCH/DELETE/standing), Hosted Link, and notable-transactions are all built + wired (earlier nulls were Plaid sandbox sparsity, not gaps). Work done this pass:
- **LLM transaction-sign fix** — `assembleBudgetContext` now labels each transaction `direction: inflow/outflow` + a convention legend, and `prompts.ts` has an AMOUNT CONVENTION block. Verified live: the model now correctly calls the +$500 United Airlines row an inflow (previously called it "spending").
- **Banks tab backend (newly found gap)** — `GET /api/banks` (per-account cards: balance/mask/status/last-synced, manual account handled) and `GET /api/banks/[id]/transactions` (per-account full history, paginated, ownership-scoped). Both verified live. Needed before the Banks frontend.
- **Live Google Sheets + Excel import** — new `spreadsheet_connections` table (encrypted tokens, migrated), shared `normalizeMappedRows` (CSV route refactored onto it, regression-verified), `googleapis` Sheets path (expiry_date refresh-bug guard + token-rotation persistence), MSAL serialized-token-cache Excel/Graph path. Routes: `/api/import/{google,excel}/{start,callback,sync}`. Code built + smoke-tested (503 not-configured, 409 not-connected, 401 unauth, typecheck clean). **Live OAuth flows NOT yet verified — blocked on the user's Google Cloud + Microsoft Entra console setup + credentials** (`GOOGLE_OAUTH_CLIENT_ID/SECRET`, `MS_OAUTH_CLIENT_ID/SECRET/TENANT` in `.env.local`).

**Known open items:**
- Live import OAuth verification — pending the console setup above (the one backend item that needs external OAuth infra only the user can provision).
- `ALPHA_VANTAGE_API_KEY` still empty in `.env.local` (OHLCV fallback only; Yahoo primary works). `ANTHROPIC_API_KEY` + SnapTrade keys are now populated and verified.
- Nightly 2am job: verified via manual trigger (batch submit accepted after the custom_id fix); batch *collection* left to the live `batch_poll` cron.
- Frontend: fully untouched — Bearer-auth client wiring, account-connection UI, onboarding wizard, Holding Detail, Banks tab UI (backend now ready for it). The next major chunk once backend is fully closed out.

**Key env file:** `backend/.env.local` — populated for auth (`ENCRYPTION_KEY`, `BETTER_AUTH_SECRET`, `SEED_EMAIL`/`SEED_PASSWORD` — see `~/.secrets/finance-dashboard-local-dev-seed.txt`) and Plaid Sandbox (`PLAID_CLIENT_ID`/`PLAID_SECRET`, from `~/.secrets/Plaid.txt`). Gitignored, local to each machine — Windows and Linux checkouts need this filled in separately.

**Handoff doc (start here in a new session):**
`~/.claude/projects/C--Users-bchue/wayfinder/handoff-2026-07-16.md`

**Build reminders (deferred items — must surface at build time and post-launch):**
See `~/.claude/projects/C--Users-bchue/wayfinder/build-reminders.md` — contains 8 items that MUST be called out at the relevant build phase or post-launch. Do not begin a build session without checking this file.

See memory file: `finance-dashboard-wayfinder.md`

## Standing Preferences

- Research standard: before any decision is locked, research must cover known bugs, scaling issues, runtime gotchas, hidden advantages, and prior art from publicly available projects. No shallow answers.
- Research subagents run in parallel and are given full time to do deep dives.
- Post-completion analysis: once all wayfinder tickets are resolved, conduct a full analysis of every completed ticket — reviewing the reasoning applied, the research methods used, gaps in coverage, and opportunities missed. This is a mandatory final step before the spec is written.
- Wayfinder tracker: local-markdown (no GitHub repo for this project). Map and tickets live in `~/.claude/projects/C--Users-bchue/wayfinder/`. Research findings live in `wayfinder/research/`.
- Claude Code pinned at v2.1.81 — do not suggest updating (breaks Wispr Flow voice paste on Windows).
